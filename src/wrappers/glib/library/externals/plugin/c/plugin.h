/*
** This file have been created by wrapper-generator.
** Any change will be lost by the next execution of the tool.
*/
#define gsource_callback_funcs_struct_get_ref(a_structure) (((_GSourceCallbackFuncs*) a_structure).ref)

#define gsource_callback_funcs_struct_set_ref(a_structure,a_value) ((_GSourceCallbackFuncs)->ref = a_value;

#define gsource_callback_funcs_struct_get_unref(a_structure) (((_GSourceCallbackFuncs*) a_structure).unref)

#define gsource_callback_funcs_struct_set_unref(a_structure,a_value) ((_GSourceCallbackFuncs)->unref = a_value;

#define gsource_callback_funcs_struct_get_get(a_structure) (((_GSourceCallbackFuncs*) a_structure).get)

#define gsource_callback_funcs_struct_set_get(a_structure,a_value) ((_GSourceCallbackFuncs)->get = a_value;

#define gtest_log_msg_struct_get_log_type(a_structure) (((GTestLogMsg*) a_structure).log_type)

#define gtest_log_msg_struct_set_log_type(a_structure,a_value) ((GTestLogMsg)->log_type = a_value;

#define gtest_log_msg_struct_get_n_strings(a_structure) (((GTestLogMsg*) a_structure).n_strings)

#define gtest_log_msg_struct_set_n_strings(a_structure,a_value) ((GTestLogMsg)->n_strings = a_value;

#define gtest_log_msg_struct_get_strings(a_structure) (((GTestLogMsg*) a_structure).strings)

#define gtest_log_msg_struct_set_strings(a_structure,a_value) ((GTestLogMsg)->strings = a_value;

#define gtest_log_msg_struct_get_n_nums(a_structure) (((GTestLogMsg*) a_structure).n_nums)

#define gtest_log_msg_struct_set_n_nums(a_structure,a_value) ((GTestLogMsg)->n_nums = a_value;

#define gtest_log_msg_struct_get_nums(a_structure) (((GTestLogMsg*) a_structure).nums)

#define gtest_log_msg_struct_set_nums(a_structure,a_value) ((GTestLogMsg)->nums = a_value;

#define gpoll_fd_struct_get_fd(a_structure) (((_GPollFD*) a_structure).fd)

#define gpoll_fd_struct_set_fd(a_structure,a_value) ((_GPollFD)->fd = a_value;

#define gpoll_fd_struct_get_events(a_structure) (((_GPollFD*) a_structure).events)

#define gpoll_fd_struct_set_events(a_structure,a_value) ((_GPollFD)->events = a_value;

#define gpoll_fd_struct_get_revents(a_structure) (((_GPollFD*) a_structure).revents)

#define gpoll_fd_struct_set_revents(a_structure,a_value) ((_GPollFD)->revents = a_value;

#define gtest_log_buffer_struct_get_data(a_structure) (((GTestLogBuffer*) a_structure).data)

#define gtest_log_buffer_struct_set_data(a_structure,a_value) ((GTestLogBuffer)->data = a_value;

#define gtest_log_buffer_struct_get_msgs(a_structure) (((GTestLogBuffer*) a_structure).msgs)

#define gtest_log_buffer_struct_set_msgs(a_structure,a_value) ((GTestLogBuffer)->msgs = a_value;

#define gthread_struct_get_func(a_structure) (((_GThread*) a_structure).func)

#define gthread_struct_set_func(a_structure,a_value) ((_GThread)->func = a_value;

#define gthread_struct_get_data(a_structure) (((_GThread*) a_structure).data)

#define gthread_struct_set_data(a_structure,a_value) ((_GThread)->data = a_value;

#define gthread_struct_get_joinable(a_structure) (((_GThread*) a_structure).joinable)

#define gthread_struct_set_joinable(a_structure,a_value) ((_GThread)->joinable = a_value;

#define gthread_struct_get_priority(a_structure) (((_GThread*) a_structure).priority)

#define gthread_struct_set_priority(a_structure,a_value) ((_GThread)->priority = a_value;

#define gscanner_config_struct_get_cset_skip_characters(a_structure) (((_GScannerConfig*) a_structure).cset_skip_characters)

#define gscanner_config_struct_set_cset_skip_characters(a_structure,a_value) ((_GScannerConfig)->cset_skip_characters = a_value;

#define gscanner_config_struct_get_cset_identifier_first(a_structure) (((_GScannerConfig*) a_structure).cset_identifier_first)

#define gscanner_config_struct_set_cset_identifier_first(a_structure,a_value) ((_GScannerConfig)->cset_identifier_first = a_value;

#define gscanner_config_struct_get_cset_identifier_nth(a_structure) (((_GScannerConfig*) a_structure).cset_identifier_nth)

#define gscanner_config_struct_set_cset_identifier_nth(a_structure,a_value) ((_GScannerConfig)->cset_identifier_nth = a_value;

#define gscanner_config_struct_get_cpair_comment_single(a_structure) (((_GScannerConfig*) a_structure).cpair_comment_single)

#define gscanner_config_struct_set_cpair_comment_single(a_structure,a_value) ((_GScannerConfig)->cpair_comment_single = a_value;

#define gscanner_config_struct_get_case_sensitive(a_structure) (((_GScannerConfig*) a_structure).case_sensitive)

#define gscanner_config_struct_set_case_sensitive(a_structure,a_value) ((_GScannerConfig)->case_sensitive = a_value;

#define gscanner_config_struct_get_skip_comment_multi(a_structure) (((_GScannerConfig*) a_structure).skip_comment_multi)

#define gscanner_config_struct_set_skip_comment_multi(a_structure,a_value) ((_GScannerConfig)->skip_comment_multi = a_value;

#define gscanner_config_struct_get_skip_comment_single(a_structure) (((_GScannerConfig*) a_structure).skip_comment_single)

#define gscanner_config_struct_set_skip_comment_single(a_structure,a_value) ((_GScannerConfig)->skip_comment_single = a_value;

#define gscanner_config_struct_get_scan_comment_multi(a_structure) (((_GScannerConfig*) a_structure).scan_comment_multi)

#define gscanner_config_struct_set_scan_comment_multi(a_structure,a_value) ((_GScannerConfig)->scan_comment_multi = a_value;

#define gscanner_config_struct_get_scan_identifier(a_structure) (((_GScannerConfig*) a_structure).scan_identifier)

#define gscanner_config_struct_set_scan_identifier(a_structure,a_value) ((_GScannerConfig)->scan_identifier = a_value;

#define gscanner_config_struct_get_scan_identifier_1char(a_structure) (((_GScannerConfig*) a_structure).scan_identifier_1char)

#define gscanner_config_struct_set_scan_identifier_1char(a_structure,a_value) ((_GScannerConfig)->scan_identifier_1char = a_value;

#define gscanner_config_struct_get_scan_identifier_null(a_structure) (((_GScannerConfig*) a_structure).scan_identifier_NULL)

#define gscanner_config_struct_set_scan_identifier_null(a_structure,a_value) ((_GScannerConfig)->scan_identifier_NULL = a_value;

#define gscanner_config_struct_get_scan_symbols(a_structure) (((_GScannerConfig*) a_structure).scan_symbols)

#define gscanner_config_struct_set_scan_symbols(a_structure,a_value) ((_GScannerConfig)->scan_symbols = a_value;

#define gscanner_config_struct_get_scan_binary(a_structure) (((_GScannerConfig*) a_structure).scan_binary)

#define gscanner_config_struct_set_scan_binary(a_structure,a_value) ((_GScannerConfig)->scan_binary = a_value;

#define gscanner_config_struct_get_scan_octal(a_structure) (((_GScannerConfig*) a_structure).scan_octal)

#define gscanner_config_struct_set_scan_octal(a_structure,a_value) ((_GScannerConfig)->scan_octal = a_value;

#define gscanner_config_struct_get_scan_float(a_structure) (((_GScannerConfig*) a_structure).scan_float)

#define gscanner_config_struct_set_scan_float(a_structure,a_value) ((_GScannerConfig)->scan_float = a_value;

#define gscanner_config_struct_get_scan_hex(a_structure) (((_GScannerConfig*) a_structure).scan_hex)

#define gscanner_config_struct_set_scan_hex(a_structure,a_value) ((_GScannerConfig)->scan_hex = a_value;

#define gscanner_config_struct_get_scan_hex_dollar(a_structure) (((_GScannerConfig*) a_structure).scan_hex_dollar)

#define gscanner_config_struct_set_scan_hex_dollar(a_structure,a_value) ((_GScannerConfig)->scan_hex_dollar = a_value;

#define gscanner_config_struct_get_scan_string_sq(a_structure) (((_GScannerConfig*) a_structure).scan_string_sq)

#define gscanner_config_struct_set_scan_string_sq(a_structure,a_value) ((_GScannerConfig)->scan_string_sq = a_value;

#define gscanner_config_struct_get_scan_string_dq(a_structure) (((_GScannerConfig*) a_structure).scan_string_dq)

#define gscanner_config_struct_set_scan_string_dq(a_structure,a_value) ((_GScannerConfig)->scan_string_dq = a_value;

#define gscanner_config_struct_get_numbers_2_int(a_structure) (((_GScannerConfig*) a_structure).numbers_2_int)

#define gscanner_config_struct_set_numbers_2_int(a_structure,a_value) ((_GScannerConfig)->numbers_2_int = a_value;

#define gscanner_config_struct_get_int_2_float(a_structure) (((_GScannerConfig*) a_structure).int_2_float)

#define gscanner_config_struct_set_int_2_float(a_structure,a_value) ((_GScannerConfig)->int_2_float = a_value;

#define gscanner_config_struct_get_identifier_2_string(a_structure) (((_GScannerConfig*) a_structure).identifier_2_string)

#define gscanner_config_struct_set_identifier_2_string(a_structure,a_value) ((_GScannerConfig)->identifier_2_string = a_value;

#define gscanner_config_struct_get_char_2_token(a_structure) (((_GScannerConfig*) a_structure).char_2_token)

#define gscanner_config_struct_set_char_2_token(a_structure,a_value) ((_GScannerConfig)->char_2_token = a_value;

#define gscanner_config_struct_get_symbol_2_token(a_structure) (((_GScannerConfig*) a_structure).symbol_2_token)

#define gscanner_config_struct_set_symbol_2_token(a_structure,a_value) ((_GScannerConfig)->symbol_2_token = a_value;

#define gscanner_config_struct_get_scope_0_fallback(a_structure) (((_GScannerConfig*) a_structure).scope_0_fallback)

#define gscanner_config_struct_set_scope_0_fallback(a_structure,a_value) ((_GScannerConfig)->scope_0_fallback = a_value;

#define gscanner_config_struct_get_store_int64(a_structure) (((_GScannerConfig*) a_structure).store_int64)

#define gscanner_config_struct_set_store_int64(a_structure,a_value) ((_GScannerConfig)->store_int64 = a_value;

#define gscanner_config_struct_get_padding_dummy(a_structure) (((_GScannerConfig*) a_structure).padding_dummy)

#define gscanner_config_struct_set_padding_dummy(a_structure,a_value) ((_GScannerConfig)->padding_dummy = a_value;

#define gsource_struct_get_callback_data(a_structure) (((_GSource*) a_structure).callback_data)

#define gsource_struct_set_callback_data(a_structure,a_value) ((_GSource)->callback_data = a_value;

#define gsource_struct_get_callback_funcs(a_structure) (((_GSource*) a_structure).callback_funcs)

#define gsource_struct_set_callback_funcs(a_structure,a_value) ((_GSource)->callback_funcs = a_value;

#define gsource_struct_get_source_funcs(a_structure) (((_GSource*) a_structure).source_funcs)

#define gsource_struct_set_source_funcs(a_structure,a_value) ((_GSource)->source_funcs = a_value;

#define gsource_struct_get_ref_count(a_structure) (((_GSource*) a_structure).ref_count)

#define gsource_struct_set_ref_count(a_structure,a_value) ((_GSource)->ref_count = a_value;

#define gsource_struct_get_context(a_structure) (((_GSource*) a_structure).context)

#define gsource_struct_set_context(a_structure,a_value) ((_GSource)->context = a_value;

#define gsource_struct_get_priority(a_structure) (((_GSource*) a_structure).priority)

#define gsource_struct_set_priority(a_structure,a_value) ((_GSource)->priority = a_value;

#define gsource_struct_get_flags(a_structure) (((_GSource*) a_structure).flags)

#define gsource_struct_set_flags(a_structure,a_value) ((_GSource)->flags = a_value;

#define gsource_struct_get_source_id(a_structure) (((_GSource*) a_structure).source_id)

#define gsource_struct_set_source_id(a_structure,a_value) ((_GSource)->source_id = a_value;

#define gsource_struct_get_poll_fds(a_structure) (((_GSource*) a_structure).poll_fds)

#define gsource_struct_set_poll_fds(a_structure,a_value) ((_GSource)->poll_fds = a_value;

#define gsource_struct_get_prev(a_structure) (((_GSource*) a_structure).prev)

#define gsource_struct_set_prev(a_structure,a_value) ((_GSource)->prev = a_value;

#define gsource_struct_get_next(a_structure) (((_GSource*) a_structure).next)

#define gsource_struct_set_next(a_structure,a_value) ((_GSource)->next = a_value;

#define gsource_struct_get_reserved1(a_structure) (((_GSource*) a_structure).reserved1)

#define gsource_struct_set_reserved1(a_structure,a_value) ((_GSource)->reserved1 = a_value;

#define gsource_struct_get_reserved2(a_structure) (((_GSource*) a_structure).reserved2)

#define gsource_struct_set_reserved2(a_structure,a_value) ((_GSource)->reserved2 = a_value;

#define gdebug_key_struct_get_key(a_structure) (((_GDebugKey*) a_structure).key)

#define gdebug_key_struct_set_key(a_structure,a_value) ((_GDebugKey)->key = a_value;

#define gdebug_key_struct_get_value(a_structure) (((_GDebugKey*) a_structure).value)

#define gdebug_key_struct_set_value(a_structure,a_value) ((_GDebugKey)->value = a_value;

#define gmem_vtable_struct_get_malloc(a_structure) (((_GMemVTable*) a_structure).malloc)

#define gmem_vtable_struct_set_malloc(a_structure,a_value) ((_GMemVTable)->malloc = a_value;

#define gmem_vtable_struct_get_realloc(a_structure) (((_GMemVTable*) a_structure).realloc)

#define gmem_vtable_struct_set_realloc(a_structure,a_value) ((_GMemVTable)->realloc = a_value;

#define gmem_vtable_struct_get_free(a_structure) (((_GMemVTable*) a_structure).free)

#define gmem_vtable_struct_set_free(a_structure,a_value) ((_GMemVTable)->free = a_value;

#define gmem_vtable_struct_get_calloc(a_structure) (((_GMemVTable*) a_structure).calloc)

#define gmem_vtable_struct_set_calloc(a_structure,a_value) ((_GMemVTable)->calloc = a_value;

#define gmem_vtable_struct_get_try_malloc(a_structure) (((_GMemVTable*) a_structure).try_malloc)

#define gmem_vtable_struct_set_try_malloc(a_structure,a_value) ((_GMemVTable)->try_malloc = a_value;

#define gmem_vtable_struct_get_try_realloc(a_structure) (((_GMemVTable*) a_structure).try_realloc)

#define gmem_vtable_struct_set_try_realloc(a_structure,a_value) ((_GMemVTable)->try_realloc = a_value;

#define gtrash_stack_struct_get_next(a_structure) (((_GTrashStack*) a_structure).next)

#define gtrash_stack_struct_set_next(a_structure,a_value) ((_GTrashStack)->next = a_value;

#define gsource_funcs_struct_get_prepare(a_structure) (((_GSourceFuncs*) a_structure).prepare)

#define gsource_funcs_struct_set_prepare(a_structure,a_value) ((_GSourceFuncs)->prepare = a_value;

#define gsource_funcs_struct_get_check_external(a_structure) (((_GSourceFuncs*) a_structure).check)

#define gsource_funcs_struct_set_check_external(a_structure,a_value) ((_GSourceFuncs)->check = a_value;

#define gsource_funcs_struct_get_dispatch(a_structure) (((_GSourceFuncs*) a_structure).dispatch)

#define gsource_funcs_struct_set_dispatch(a_structure,a_value) ((_GSourceFuncs)->dispatch = a_value;

#define gsource_funcs_struct_get_finalize(a_structure) (((_GSourceFuncs*) a_structure).finalize)

#define gsource_funcs_struct_set_finalize(a_structure,a_value) ((_GSourceFuncs)->finalize = a_value;

#define gsource_funcs_struct_get_closure_callback(a_structure) (((_GSourceFuncs*) a_structure).closure_callback)

#define gsource_funcs_struct_set_closure_callback(a_structure,a_value) ((_GSourceFuncs)->closure_callback = a_value;

#define gsource_funcs_struct_get_closure_marshal(a_structure) (((_GSourceFuncs*) a_structure).closure_marshal)

#define gsource_funcs_struct_set_closure_marshal(a_structure,a_value) ((_GSourceFuncs)->closure_marshal = a_value;

#define gcompletion_struct_get_items(a_structure) (((_GCompletion*) a_structure).items)

#define gcompletion_struct_set_items(a_structure,a_value) ((_GCompletion)->items = a_value;

#define gcompletion_struct_get_func(a_structure) (((_GCompletion*) a_structure).func)

#define gcompletion_struct_set_func(a_structure,a_value) ((_GCompletion)->func = a_value;

#define gcompletion_struct_get_prefix(a_structure) (((_GCompletion*) a_structure).prefix)

#define gcompletion_struct_set_prefix(a_structure,a_value) ((_GCompletion)->prefix = a_value;

#define gcompletion_struct_get_cache(a_structure) (((_GCompletion*) a_structure).cache)

#define gcompletion_struct_set_cache(a_structure,a_value) ((_GCompletion)->cache = a_value;

#define gcompletion_struct_get_strncmp_func(a_structure) (((_GCompletion*) a_structure).strncmp_func)

#define gcompletion_struct_set_strncmp_func(a_structure,a_value) ((_GCompletion)->strncmp_func = a_value;

#define gvariant_builder_struct_get_x(a_structure) (((_GVariantBuilder*) a_structure).x)

#define gvariant_builder_struct_set_x(a_structure,a_value) ((_GVariantBuilder)->x = a_value;

#define ghook_struct_get_data(a_structure) (((_GHook*) a_structure).data)

#define ghook_struct_set_data(a_structure,a_value) ((_GHook)->data = a_value;

#define ghook_struct_get_next(a_structure) (((_GHook*) a_structure).next)

#define ghook_struct_set_next(a_structure,a_value) ((_GHook)->next = a_value;

#define ghook_struct_get_prev(a_structure) (((_GHook*) a_structure).prev)

#define ghook_struct_set_prev(a_structure,a_value) ((_GHook)->prev = a_value;

#define ghook_struct_get_ref_count(a_structure) (((_GHook*) a_structure).ref_count)

#define ghook_struct_set_ref_count(a_structure,a_value) ((_GHook)->ref_count = a_value;

#define ghook_struct_get_hook_id(a_structure) (((_GHook*) a_structure).hook_id)

#define ghook_struct_set_hook_id(a_structure,a_value) ((_GHook)->hook_id = a_value;

#define ghook_struct_get_flags(a_structure) (((_GHook*) a_structure).flags)

#define ghook_struct_set_flags(a_structure,a_value) ((_GHook)->flags = a_value;

#define ghook_struct_get_func(a_structure) (((_GHook*) a_structure).func)

#define ghook_struct_set_func(a_structure,a_value) ((_GHook)->func = a_value;

#define ghook_struct_get_destroy(a_structure) (((_GHook*) a_structure).destroy)

#define ghook_struct_set_destroy(a_structure,a_value) ((_GHook)->destroy = a_value;

#define goption_entry_struct_get_long_name(a_structure) (((_GOptionEntry*) a_structure).long_name)

#define goption_entry_struct_set_long_name(a_structure,a_value) ((_GOptionEntry)->long_name = a_value;

#define goption_entry_struct_get_short_name(a_structure) (((_GOptionEntry*) a_structure).short_name)

#define goption_entry_struct_set_short_name(a_structure,a_value) ((_GOptionEntry)->short_name = a_value;

#define goption_entry_struct_get_flags(a_structure) (((_GOptionEntry*) a_structure).flags)

#define goption_entry_struct_set_flags(a_structure,a_value) ((_GOptionEntry)->flags = a_value;

#define goption_entry_struct_get_arg(a_structure) (((_GOptionEntry*) a_structure).arg)

#define goption_entry_struct_set_arg(a_structure,a_value) ((_GOptionEntry)->arg = a_value;

#define goption_entry_struct_get_arg_data(a_structure) (((_GOptionEntry*) a_structure).arg_data)

#define goption_entry_struct_set_arg_data(a_structure,a_value) ((_GOptionEntry)->arg_data = a_value;

#define goption_entry_struct_get_description(a_structure) (((_GOptionEntry*) a_structure).description)

#define goption_entry_struct_set_description(a_structure,a_value) ((_GOptionEntry)->description = a_value;

#define goption_entry_struct_get_arg_description(a_structure) (((_GOptionEntry*) a_structure).arg_description)

#define goption_entry_struct_set_arg_description(a_structure,a_value) ((_GOptionEntry)->arg_description = a_value;

#define gonce_struct_get_status(a_structure) (((_GOnce*) a_structure).status)

#define gonce_struct_set_status(a_structure,a_value) ((_GOnce)->status = a_value;

#define gonce_struct_get_retval(a_structure) (((_GOnce*) a_structure).retval)

#define gonce_struct_set_retval(a_structure,a_value) ((_GOnce)->retval = a_value;

#define ghash_table_iter_struct_get_dummy1(a_structure) (((_GHashTableIter*) a_structure).dummy1)

#define ghash_table_iter_struct_set_dummy1(a_structure,a_value) ((_GHashTableIter)->dummy1 = a_value;

#define ghash_table_iter_struct_get_dummy2(a_structure) (((_GHashTableIter*) a_structure).dummy2)

#define ghash_table_iter_struct_set_dummy2(a_structure,a_value) ((_GHashTableIter)->dummy2 = a_value;

#define ghash_table_iter_struct_get_dummy3(a_structure) (((_GHashTableIter*) a_structure).dummy3)

#define ghash_table_iter_struct_set_dummy3(a_structure,a_value) ((_GHashTableIter)->dummy3 = a_value;

#define ghash_table_iter_struct_get_dummy4(a_structure) (((_GHashTableIter*) a_structure).dummy4)

#define ghash_table_iter_struct_set_dummy4(a_structure,a_value) ((_GHashTableIter)->dummy4 = a_value;

#define ghash_table_iter_struct_get_dummy5(a_structure) (((_GHashTableIter*) a_structure).dummy5)

#define ghash_table_iter_struct_set_dummy5(a_structure,a_value) ((_GHashTableIter)->dummy5 = a_value;

#define ghash_table_iter_struct_get_dummy6(a_structure) (((_GHashTableIter*) a_structure).dummy6)

#define ghash_table_iter_struct_set_dummy6(a_structure,a_value) ((_GHashTableIter)->dummy6 = a_value;

#define gptr_array_struct_get_pdata(a_structure) (((_GPtrArray*) a_structure).pdata)

#define gptr_array_struct_set_pdata(a_structure,a_value) ((_GPtrArray)->pdata = a_value;

#define gptr_array_struct_get_len(a_structure) (((_GPtrArray*) a_structure).len)

#define gptr_array_struct_set_len(a_structure,a_value) ((_GPtrArray)->len = a_value;

#define gmarkup_parser_struct_get_start_element(a_structure) (((_GMarkupParser*) a_structure).start_element)

#define gmarkup_parser_struct_set_start_element(a_structure,a_value) ((_GMarkupParser)->start_element = a_value;

#define gmarkup_parser_struct_get_end_element(a_structure) (((_GMarkupParser*) a_structure).end_element)

#define gmarkup_parser_struct_set_end_element(a_structure,a_value) ((_GMarkupParser)->end_element = a_value;

#define gmarkup_parser_struct_get_text(a_structure) (((_GMarkupParser*) a_structure).text)

#define gmarkup_parser_struct_set_text(a_structure,a_value) ((_GMarkupParser)->text = a_value;

#define gmarkup_parser_struct_get_passthrough(a_structure) (((_GMarkupParser*) a_structure).passthrough)

#define gmarkup_parser_struct_set_passthrough(a_structure,a_value) ((_GMarkupParser)->passthrough = a_value;

#define gmarkup_parser_struct_get_error(a_structure) (((_GMarkupParser*) a_structure).error)

#define gmarkup_parser_struct_set_error(a_structure,a_value) ((_GMarkupParser)->error = a_value;

#define gqueue_struct_get_head(a_structure) (((_GQueue*) a_structure).head)

#define gqueue_struct_set_head(a_structure,a_value) ((_GQueue)->head = a_value;

#define gqueue_struct_get_tail(a_structure) (((_GQueue*) a_structure).tail)

#define gqueue_struct_set_tail(a_structure,a_value) ((_GQueue)->tail = a_value;

#define gqueue_struct_get_length(a_structure) (((_GQueue*) a_structure).length)

#define gqueue_struct_set_length(a_structure,a_value) ((_GQueue)->length = a_value;

#define gtest_config_struct_get_test_initialized(a_structure) (((GTestConfig*) a_structure).test_initialized)

#define gtest_config_struct_set_test_initialized(a_structure,a_value) ((GTestConfig)->test_initialized = a_value;

#define gtest_config_struct_get_test_quick(a_structure) (((GTestConfig*) a_structure).test_quick)

#define gtest_config_struct_set_test_quick(a_structure,a_value) ((GTestConfig)->test_quick = a_value;

#define gtest_config_struct_get_test_perf(a_structure) (((GTestConfig*) a_structure).test_perf)

#define gtest_config_struct_set_test_perf(a_structure,a_value) ((GTestConfig)->test_perf = a_value;

#define gtest_config_struct_get_test_verbose(a_structure) (((GTestConfig*) a_structure).test_verbose)

#define gtest_config_struct_set_test_verbose(a_structure,a_value) ((GTestConfig)->test_verbose = a_value;

#define gtest_config_struct_get_test_quiet(a_structure) (((GTestConfig*) a_structure).test_quiet)

#define gtest_config_struct_set_test_quiet(a_structure,a_value) ((GTestConfig)->test_quiet = a_value;

#define gstring_struct_get_str(a_structure) (((_GString*) a_structure).str)

#define gstring_struct_set_str(a_structure,a_value) ((_GString)->str = a_value;

#define gstring_struct_get_len(a_structure) (((_GString*) a_structure).len)

#define gstring_struct_set_len(a_structure,a_value) ((_GString)->len = a_value;

#define gstring_struct_get_allocated_len(a_structure) (((_GString*) a_structure).allocated_len)

#define gstring_struct_set_allocated_len(a_structure,a_value) ((_GString)->allocated_len = a_value;

#define giofuncs_struct_get_io_read(a_structure) (((_GIOFuncs*) a_structure).io_read)

#define giofuncs_struct_set_io_read(a_structure,a_value) ((_GIOFuncs)->io_read = a_value;

#define giofuncs_struct_get_io_write(a_structure) (((_GIOFuncs*) a_structure).io_write)

#define giofuncs_struct_set_io_write(a_structure,a_value) ((_GIOFuncs)->io_write = a_value;

#define giofuncs_struct_get_io_seek(a_structure) (((_GIOFuncs*) a_structure).io_seek)

#define giofuncs_struct_set_io_seek(a_structure,a_value) ((_GIOFuncs)->io_seek = a_value;

#define giofuncs_struct_get_io_close(a_structure) (((_GIOFuncs*) a_structure).io_close)

#define giofuncs_struct_set_io_close(a_structure,a_value) ((_GIOFuncs)->io_close = a_value;

#define giofuncs_struct_get_io_create_watch(a_structure) (((_GIOFuncs*) a_structure).io_create_watch)

#define giofuncs_struct_set_io_create_watch(a_structure,a_value) ((_GIOFuncs)->io_create_watch = a_value;

#define giofuncs_struct_get_io_free(a_structure) (((_GIOFuncs*) a_structure).io_free)

#define giofuncs_struct_set_io_free(a_structure,a_value) ((_GIOFuncs)->io_free = a_value;

#define giofuncs_struct_get_io_set_flags(a_structure) (((_GIOFuncs*) a_structure).io_set_flags)

#define giofuncs_struct_set_io_set_flags(a_structure,a_value) ((_GIOFuncs)->io_set_flags = a_value;

#define giofuncs_struct_get_io_get_flags(a_structure) (((_GIOFuncs*) a_structure).io_get_flags)

#define giofuncs_struct_set_io_get_flags(a_structure,a_value) ((_GIOFuncs)->io_get_flags = a_value;

#define gdate_struct_get_julian_days(a_structure) (((_GDate*) a_structure).julian_days)

#define gdate_struct_set_julian_days(a_structure,a_value) ((_GDate)->julian_days = a_value;

#define gdate_struct_get_julian(a_structure) (((_GDate*) a_structure).julian)

#define gdate_struct_set_julian(a_structure,a_value) ((_GDate)->julian = a_value;

#define gdate_struct_get_dmy(a_structure) (((_GDate*) a_structure).dmy)

#define gdate_struct_set_dmy(a_structure,a_value) ((_GDate)->dmy = a_value;

#define gdate_struct_get_day(a_structure) (((_GDate*) a_structure).day)

#define gdate_struct_set_day(a_structure,a_value) ((_GDate)->day = a_value;

#define gdate_struct_get_month(a_structure) (((_GDate*) a_structure).month)

#define gdate_struct_set_month(a_structure,a_value) ((_GDate)->month = a_value;

#define gdate_struct_get_year(a_structure) (((_GDate*) a_structure).year)

#define gdate_struct_set_year(a_structure,a_value) ((_GDate)->year = a_value;

#define gstatic_rwlock_struct_get_read_cond(a_structure) (((_GStaticRWLock*) a_structure).read_cond)

#define gstatic_rwlock_struct_set_read_cond(a_structure,a_value) ((_GStaticRWLock)->read_cond = a_value;

#define gstatic_rwlock_struct_get_write_cond(a_structure) (((_GStaticRWLock*) a_structure).write_cond)

#define gstatic_rwlock_struct_set_write_cond(a_structure,a_value) ((_GStaticRWLock)->write_cond = a_value;

#define gstatic_rwlock_struct_get_read_counter(a_structure) (((_GStaticRWLock*) a_structure).read_counter)

#define gstatic_rwlock_struct_set_read_counter(a_structure,a_value) ((_GStaticRWLock)->read_counter = a_value;

#define gstatic_rwlock_struct_get_have_writer(a_structure) (((_GStaticRWLock*) a_structure).have_writer)

#define gstatic_rwlock_struct_set_have_writer(a_structure,a_value) ((_GStaticRWLock)->have_writer = a_value;

#define gstatic_rwlock_struct_get_want_to_read(a_structure) (((_GStaticRWLock*) a_structure).want_to_read)

#define gstatic_rwlock_struct_set_want_to_read(a_structure,a_value) ((_GStaticRWLock)->want_to_read = a_value;

#define gstatic_rwlock_struct_get_want_to_write(a_structure) (((_GStaticRWLock*) a_structure).want_to_write)

#define gstatic_rwlock_struct_set_want_to_write(a_structure,a_value) ((_GStaticRWLock)->want_to_write = a_value;

#define gstatic_private_struct_get_index(a_structure) (((_GStaticPrivate*) a_structure).index)

#define gstatic_private_struct_set_index(a_structure,a_value) ((_GStaticPrivate)->index = a_value;

#define giochannel_struct_get_ref_count(a_structure) (((_GIOChannel*) a_structure).ref_count)

#define giochannel_struct_set_ref_count(a_structure,a_value) ((_GIOChannel)->ref_count = a_value;

#define giochannel_struct_get_funcs(a_structure) (((_GIOChannel*) a_structure).funcs)

#define giochannel_struct_set_funcs(a_structure,a_value) ((_GIOChannel)->funcs = a_value;

#define giochannel_struct_get_encoding(a_structure) (((_GIOChannel*) a_structure).encoding)

#define giochannel_struct_set_encoding(a_structure,a_value) ((_GIOChannel)->encoding = a_value;

#define giochannel_struct_get_read_cd(a_structure) (((_GIOChannel*) a_structure).read_cd)

#define giochannel_struct_set_read_cd(a_structure,a_value) ((_GIOChannel)->read_cd = a_value;

#define giochannel_struct_get_write_cd(a_structure) (((_GIOChannel*) a_structure).write_cd)

#define giochannel_struct_set_write_cd(a_structure,a_value) ((_GIOChannel)->write_cd = a_value;

#define giochannel_struct_get_line_term(a_structure) (((_GIOChannel*) a_structure).line_term)

#define giochannel_struct_set_line_term(a_structure,a_value) ((_GIOChannel)->line_term = a_value;

#define giochannel_struct_get_line_term_len(a_structure) (((_GIOChannel*) a_structure).line_term_len)

#define giochannel_struct_set_line_term_len(a_structure,a_value) ((_GIOChannel)->line_term_len = a_value;

#define giochannel_struct_get_buf_size(a_structure) (((_GIOChannel*) a_structure).buf_size)

#define giochannel_struct_set_buf_size(a_structure,a_value) ((_GIOChannel)->buf_size = a_value;

#define giochannel_struct_get_read_buf(a_structure) (((_GIOChannel*) a_structure).read_buf)

#define giochannel_struct_set_read_buf(a_structure,a_value) ((_GIOChannel)->read_buf = a_value;

#define giochannel_struct_get_encoded_read_buf(a_structure) (((_GIOChannel*) a_structure).encoded_read_buf)

#define giochannel_struct_set_encoded_read_buf(a_structure,a_value) ((_GIOChannel)->encoded_read_buf = a_value;

#define giochannel_struct_get_write_buf(a_structure) (((_GIOChannel*) a_structure).write_buf)

#define giochannel_struct_set_write_buf(a_structure,a_value) ((_GIOChannel)->write_buf = a_value;

#define giochannel_struct_get_partial_write_buf(a_structure) (((_GIOChannel*) a_structure).partial_write_buf)

#define giochannel_struct_set_partial_write_buf(a_structure,a_value) ((_GIOChannel)->partial_write_buf = a_value;

#define giochannel_struct_get_use_buffer(a_structure) (((_GIOChannel*) a_structure).use_buffer)

#define giochannel_struct_set_use_buffer(a_structure,a_value) ((_GIOChannel)->use_buffer = a_value;

#define giochannel_struct_get_do_encode(a_structure) (((_GIOChannel*) a_structure).do_encode)

#define giochannel_struct_set_do_encode(a_structure,a_value) ((_GIOChannel)->do_encode = a_value;

#define giochannel_struct_get_close_on_unref(a_structure) (((_GIOChannel*) a_structure).close_on_unref)

#define giochannel_struct_set_close_on_unref(a_structure,a_value) ((_GIOChannel)->close_on_unref = a_value;

#define giochannel_struct_get_is_readable(a_structure) (((_GIOChannel*) a_structure).is_readable)

#define giochannel_struct_set_is_readable(a_structure,a_value) ((_GIOChannel)->is_readable = a_value;

#define giochannel_struct_get_is_writeable(a_structure) (((_GIOChannel*) a_structure).is_writeable)

#define giochannel_struct_set_is_writeable(a_structure,a_value) ((_GIOChannel)->is_writeable = a_value;

#define giochannel_struct_get_is_seekable(a_structure) (((_GIOChannel*) a_structure).is_seekable)

#define giochannel_struct_set_is_seekable(a_structure,a_value) ((_GIOChannel)->is_seekable = a_value;

#define giochannel_struct_get_reserved1(a_structure) (((_GIOChannel*) a_structure).reserved1)

#define giochannel_struct_set_reserved1(a_structure,a_value) ((_GIOChannel)->reserved1 = a_value;

#define giochannel_struct_get_reserved2(a_structure) (((_GIOChannel*) a_structure).reserved2)

#define giochannel_struct_set_reserved2(a_structure,a_value) ((_GIOChannel)->reserved2 = a_value;

#define ghook_list_struct_get_seq_id(a_structure) (((_GHookList*) a_structure).seq_id)

#define ghook_list_struct_set_seq_id(a_structure,a_value) ((_GHookList)->seq_id = a_value;

#define ghook_list_struct_get_hook_size(a_structure) (((_GHookList*) a_structure).hook_size)

#define ghook_list_struct_set_hook_size(a_structure,a_value) ((_GHookList)->hook_size = a_value;

#define ghook_list_struct_get_is_setup(a_structure) (((_GHookList*) a_structure).is_setup)

#define ghook_list_struct_set_is_setup(a_structure,a_value) ((_GHookList)->is_setup = a_value;

#define ghook_list_struct_get_hooks(a_structure) (((_GHookList*) a_structure).hooks)

#define ghook_list_struct_set_hooks(a_structure,a_value) ((_GHookList)->hooks = a_value;

#define ghook_list_struct_get_dummy3(a_structure) (((_GHookList*) a_structure).dummy3)

#define ghook_list_struct_set_dummy3(a_structure,a_value) ((_GHookList)->dummy3 = a_value;

#define ghook_list_struct_get_finalize_hook(a_structure) (((_GHookList*) a_structure).finalize_hook)

#define ghook_list_struct_set_finalize_hook(a_structure,a_value) ((_GHookList)->finalize_hook = a_value;

#define ghook_list_struct_get_dummy(a_structure) (((_GHookList*) a_structure).dummy)

#define ghook_list_struct_set_dummy(a_structure,a_value) ((_GHookList)->dummy = a_value;

#define gstatic_rec_mutex_struct_get_depth(a_structure) (((_GStaticRecMutex*) a_structure).depth)

#define gstatic_rec_mutex_struct_set_depth(a_structure,a_value) ((_GStaticRecMutex)->depth = a_value;

#define gscanner_struct_get_user_data(a_structure) (((_GScanner*) a_structure).user_data)

#define gscanner_struct_set_user_data(a_structure,a_value) ((_GScanner)->user_data = a_value;

#define gscanner_struct_get_max_parse_errors(a_structure) (((_GScanner*) a_structure).max_parse_errors)

#define gscanner_struct_set_max_parse_errors(a_structure,a_value) ((_GScanner)->max_parse_errors = a_value;

#define gscanner_struct_get_parse_errors(a_structure) (((_GScanner*) a_structure).parse_errors)

#define gscanner_struct_set_parse_errors(a_structure,a_value) ((_GScanner)->parse_errors = a_value;

#define gscanner_struct_get_input_name(a_structure) (((_GScanner*) a_structure).input_name)

#define gscanner_struct_set_input_name(a_structure,a_value) ((_GScanner)->input_name = a_value;

#define gscanner_struct_get_qdata(a_structure) (((_GScanner*) a_structure).qdata)

#define gscanner_struct_set_qdata(a_structure,a_value) ((_GScanner)->qdata = a_value;

#define gscanner_struct_get_config(a_structure) (((_GScanner*) a_structure).config)

#define gscanner_struct_set_config(a_structure,a_value) ((_GScanner)->config = a_value;

#define gscanner_struct_get_token(a_structure) (((_GScanner*) a_structure).token)

#define gscanner_struct_set_token(a_structure,a_value) ((_GScanner)->token = a_value;

#define gscanner_struct_get_line(a_structure) (((_GScanner*) a_structure).line)

#define gscanner_struct_set_line(a_structure,a_value) ((_GScanner)->line = a_value;

#define gscanner_struct_get_position(a_structure) (((_GScanner*) a_structure).position)

#define gscanner_struct_set_position(a_structure,a_value) ((_GScanner)->position = a_value;

#define gscanner_struct_get_next_token(a_structure) (((_GScanner*) a_structure).next_token)

#define gscanner_struct_set_next_token(a_structure,a_value) ((_GScanner)->next_token = a_value;

#define gscanner_struct_get_next_line(a_structure) (((_GScanner*) a_structure).next_line)

#define gscanner_struct_set_next_line(a_structure,a_value) ((_GScanner)->next_line = a_value;

#define gscanner_struct_get_next_position(a_structure) (((_GScanner*) a_structure).next_position)

#define gscanner_struct_set_next_position(a_structure,a_value) ((_GScanner)->next_position = a_value;

#define gscanner_struct_get_symbol_table(a_structure) (((_GScanner*) a_structure).symbol_table)

#define gscanner_struct_set_symbol_table(a_structure,a_value) ((_GScanner)->symbol_table = a_value;

#define gscanner_struct_get_input_fd(a_structure) (((_GScanner*) a_structure).input_fd)

#define gscanner_struct_set_input_fd(a_structure,a_value) ((_GScanner)->input_fd = a_value;

#define gscanner_struct_get_text(a_structure) (((_GScanner*) a_structure).text)

#define gscanner_struct_set_text(a_structure,a_value) ((_GScanner)->text = a_value;

#define gscanner_struct_get_text_end(a_structure) (((_GScanner*) a_structure).text_end)

#define gscanner_struct_set_text_end(a_structure,a_value) ((_GScanner)->text_end = a_value;

#define gscanner_struct_get_buffer(a_structure) (((_GScanner*) a_structure).buffer)

#define gscanner_struct_set_buffer(a_structure,a_value) ((_GScanner)->buffer = a_value;

#define gscanner_struct_get_scope_id(a_structure) (((_GScanner*) a_structure).scope_id)

#define gscanner_struct_set_scope_id(a_structure,a_value) ((_GScanner)->scope_id = a_value;

#define gscanner_struct_get_msg_handler(a_structure) (((_GScanner*) a_structure).msg_handler)

#define gscanner_struct_set_msg_handler(a_structure,a_value) ((_GScanner)->msg_handler = a_value;

#define gthread_functions_struct_get_mutex_new(a_structure) (((_GThreadFunctions*) a_structure).mutex_new)

#define gthread_functions_struct_set_mutex_new(a_structure,a_value) ((_GThreadFunctions)->mutex_new = a_value;

#define gthread_functions_struct_get_mutex_lock(a_structure) (((_GThreadFunctions*) a_structure).mutex_lock)

#define gthread_functions_struct_set_mutex_lock(a_structure,a_value) ((_GThreadFunctions)->mutex_lock = a_value;

#define gthread_functions_struct_get_mutex_trylock(a_structure) (((_GThreadFunctions*) a_structure).mutex_trylock)

#define gthread_functions_struct_set_mutex_trylock(a_structure,a_value) ((_GThreadFunctions)->mutex_trylock = a_value;

#define gthread_functions_struct_get_mutex_unlock(a_structure) (((_GThreadFunctions*) a_structure).mutex_unlock)

#define gthread_functions_struct_set_mutex_unlock(a_structure,a_value) ((_GThreadFunctions)->mutex_unlock = a_value;

#define gthread_functions_struct_get_mutex_free(a_structure) (((_GThreadFunctions*) a_structure).mutex_free)

#define gthread_functions_struct_set_mutex_free(a_structure,a_value) ((_GThreadFunctions)->mutex_free = a_value;

#define gthread_functions_struct_get_cond_new(a_structure) (((_GThreadFunctions*) a_structure).cond_new)

#define gthread_functions_struct_set_cond_new(a_structure,a_value) ((_GThreadFunctions)->cond_new = a_value;

#define gthread_functions_struct_get_cond_signal(a_structure) (((_GThreadFunctions*) a_structure).cond_signal)

#define gthread_functions_struct_set_cond_signal(a_structure,a_value) ((_GThreadFunctions)->cond_signal = a_value;

#define gthread_functions_struct_get_cond_broadcast(a_structure) (((_GThreadFunctions*) a_structure).cond_broadcast)

#define gthread_functions_struct_set_cond_broadcast(a_structure,a_value) ((_GThreadFunctions)->cond_broadcast = a_value;

#define gthread_functions_struct_get_cond_wait(a_structure) (((_GThreadFunctions*) a_structure).cond_wait)

#define gthread_functions_struct_set_cond_wait(a_structure,a_value) ((_GThreadFunctions)->cond_wait = a_value;

#define gthread_functions_struct_get_cond_timed_wait(a_structure) (((_GThreadFunctions*) a_structure).cond_timed_wait)

#define gthread_functions_struct_set_cond_timed_wait(a_structure,a_value) ((_GThreadFunctions)->cond_timed_wait = a_value;

#define gthread_functions_struct_get_cond_free(a_structure) (((_GThreadFunctions*) a_structure).cond_free)

#define gthread_functions_struct_set_cond_free(a_structure,a_value) ((_GThreadFunctions)->cond_free = a_value;

#define gthread_functions_struct_get_private_new(a_structure) (((_GThreadFunctions*) a_structure).private_new)

#define gthread_functions_struct_set_private_new(a_structure,a_value) ((_GThreadFunctions)->private_new = a_value;

#define gthread_functions_struct_get_private_get(a_structure) (((_GThreadFunctions*) a_structure).private_get)

#define gthread_functions_struct_set_private_get(a_structure,a_value) ((_GThreadFunctions)->private_get = a_value;

#define gthread_functions_struct_get_private_set(a_structure) (((_GThreadFunctions*) a_structure).private_set)

#define gthread_functions_struct_set_private_set(a_structure,a_value) ((_GThreadFunctions)->private_set = a_value;

#define gthread_functions_struct_get_thread_create(a_structure) (((_GThreadFunctions*) a_structure).thread_create)

#define gthread_functions_struct_set_thread_create(a_structure,a_value) ((_GThreadFunctions)->thread_create = a_value;

#define gthread_functions_struct_get_thread_yield(a_structure) (((_GThreadFunctions*) a_structure).thread_yield)

#define gthread_functions_struct_set_thread_yield(a_structure,a_value) ((_GThreadFunctions)->thread_yield = a_value;

#define gthread_functions_struct_get_thread_join(a_structure) (((_GThreadFunctions*) a_structure).thread_join)

#define gthread_functions_struct_set_thread_join(a_structure,a_value) ((_GThreadFunctions)->thread_join = a_value;

#define gthread_functions_struct_get_thread_exit(a_structure) (((_GThreadFunctions*) a_structure).thread_exit)

#define gthread_functions_struct_set_thread_exit(a_structure,a_value) ((_GThreadFunctions)->thread_exit = a_value;

#define gthread_functions_struct_get_thread_set_priority(a_structure) (((_GThreadFunctions*) a_structure).thread_set_priority)

#define gthread_functions_struct_set_thread_set_priority(a_structure,a_value) ((_GThreadFunctions)->thread_set_priority = a_value;

#define gthread_functions_struct_get_thread_self(a_structure) (((_GThreadFunctions*) a_structure).thread_self)

#define gthread_functions_struct_set_thread_self(a_structure,a_value) ((_GThreadFunctions)->thread_self = a_value;

#define gthread_functions_struct_get_thread_equal(a_structure) (((_GThreadFunctions*) a_structure).thread_equal)

#define gthread_functions_struct_set_thread_equal(a_structure,a_value) ((_GThreadFunctions)->thread_equal = a_value;

#define gbyte_array_struct_get_data(a_structure) (((_GByteArray*) a_structure).data)

#define gbyte_array_struct_set_data(a_structure,a_value) ((_GByteArray)->data = a_value;

#define gbyte_array_struct_get_len(a_structure) (((_GByteArray*) a_structure).len)

#define gbyte_array_struct_set_len(a_structure,a_value) ((_GByteArray)->len = a_value;

#define gslist_struct_get_data(a_structure) (((_GSList*) a_structure).data)

#define gslist_struct_set_data(a_structure,a_value) ((_GSList)->data = a_value;

#define gslist_struct_get_next(a_structure) (((_GSList*) a_structure).next)

#define gslist_struct_set_next(a_structure,a_value) ((_GSList)->next = a_value;

#define gerror_struct_get_domain(a_structure) (((_GError*) a_structure).domain)

#define gerror_struct_set_domain(a_structure,a_value) ((_GError)->domain = a_value;

#define gerror_struct_get_code(a_structure) (((_GError*) a_structure).code)

#define gerror_struct_set_code(a_structure,a_value) ((_GError)->code = a_value;

#define gerror_struct_get_message(a_structure) (((_GError*) a_structure).message)

#define gerror_struct_set_message(a_structure,a_value) ((_GError)->message = a_value;

#define gtuples_struct_get_len(a_structure) (((_GTuples*) a_structure).len)

#define gtuples_struct_set_len(a_structure,a_value) ((_GTuples)->len = a_value;

#define garray_struct_get_data(a_structure) (((_GArray*) a_structure).data)

#define garray_struct_set_data(a_structure,a_value) ((_GArray)->data = a_value;

#define garray_struct_get_len(a_structure) (((_GArray*) a_structure).len)

#define garray_struct_set_len(a_structure,a_value) ((_GArray)->len = a_value;

#define glist_struct_get_data(a_structure) (((_GList*) a_structure).data)

#define glist_struct_set_data(a_structure,a_value) ((_GList)->data = a_value;

#define glist_struct_get_next(a_structure) (((_GList*) a_structure).next)

#define glist_struct_set_next(a_structure,a_value) ((_GList)->next = a_value;

#define glist_struct_get_prev(a_structure) (((_GList*) a_structure).prev)

#define glist_struct_set_prev(a_structure,a_value) ((_GList)->prev = a_value;

#define gthread_pool_struct_get_func(a_structure) (((_GThreadPool*) a_structure).func)

#define gthread_pool_struct_set_func(a_structure,a_value) ((_GThreadPool)->func = a_value;

#define gthread_pool_struct_get_user_data(a_structure) (((_GThreadPool*) a_structure).user_data)

#define gthread_pool_struct_set_user_data(a_structure,a_value) ((_GThreadPool)->user_data = a_value;

#define gthread_pool_struct_get_exclusive(a_structure) (((_GThreadPool*) a_structure).exclusive)

#define gthread_pool_struct_set_exclusive(a_structure,a_value) ((_GThreadPool)->exclusive = a_value;

#define gtime_val_struct_get_tv_sec(a_structure) (((_GTimeVal*) a_structure).tv_sec)

#define gtime_val_struct_set_tv_sec(a_structure,a_value) ((_GTimeVal)->tv_sec = a_value;

#define gtime_val_struct_get_tv_usec(a_structure) (((_GTimeVal*) a_structure).tv_usec)

#define gtime_val_struct_set_tv_usec(a_structure,a_value) ((_GTimeVal)->tv_usec = a_value;

#define gvariant_iter_struct_get_x(a_structure) (((_GVariantIter*) a_structure).x)

#define gvariant_iter_struct_set_x(a_structure,a_value) ((_GVariantIter)->x = a_value;

#define gnode_struct_get_data(a_structure) (((_GNode*) a_structure).data)

#define gnode_struct_set_data(a_structure,a_value) ((_GNode)->data = a_value;

#define gnode_struct_get_next(a_structure) (((_GNode*) a_structure).next)

#define gnode_struct_set_next(a_structure,a_value) ((_GNode)->next = a_value;

#define gnode_struct_get_prev(a_structure) (((_GNode*) a_structure).prev)

#define gnode_struct_set_prev(a_structure,a_value) ((_GNode)->prev = a_value;

#define gnode_struct_get_parent(a_structure) (((_GNode*) a_structure).parent)

#define gnode_struct_set_parent(a_structure,a_value) ((_GNode)->parent = a_value;

#define gnode_struct_get_children(a_structure) (((_GNode*) a_structure).children)

#define gnode_struct_set_children(a_structure,a_value) ((_GNode)->children = a_value;

