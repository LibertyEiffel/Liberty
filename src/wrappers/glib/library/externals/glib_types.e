-- This file have been created by wrapper-generator.
-- Any change will be lost by the next execution of the tool.
deferred class GLIB_TYPES


insert ANY undefine is_equal, copy end

	STANDARD_C_LIBRARY_TYPES

feature {ANY} -- C type definitions (typedefs)
       gvariant_type_autoptr: POINTER
               -- typedef GVariantType_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gvariant_dict_autoptr: POINTER
               -- typedef GVariantDict_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gvariant_iter_autoptr: POINTER
               -- typedef GVariantIter_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gvariant_builder_autoptr: POINTER
               -- typedef GVariantBuilder_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gvariant_autoptr: POINTER
               -- typedef GVariant_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gtree_autoptr: POINTER
               -- typedef GTree_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gtime_zone_autoptr: POINTER
               -- typedef GTimeZone_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gtimer_autoptr: POINTER
               -- typedef GTimer_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gmutex_locker_autoptr: POINTER
               -- typedef GMutexLocker_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gthread_autoptr: POINTER
               -- typedef GThread_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gstring_chunk_autoptr: POINTER
               -- typedef GStringChunk_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gstring_autoptr: POINTER
               -- typedef GString_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gslist_autoptr: POINTER
               -- typedef GSList_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gsequence_autoptr: POINTER
               -- typedef GSequence_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gscanner_autoptr: POINTER
               -- typedef GScanner_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gmatch_info_autoptr: POINTER
               -- typedef GMatchInfo_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gregex_autoptr: POINTER
               -- typedef GRegex_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       grand_autoptr: POINTER
               -- typedef GRand_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gqueue_autoptr: POINTER
               -- typedef GQueue_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gpattern_spec_autoptr: POINTER
               -- typedef GPatternSpec_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       goption_group_autoptr: POINTER
               -- typedef GOptionGroup_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       goption_context_autoptr: POINTER
               -- typedef GOptionContext_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gnode_autoptr: POINTER
               -- typedef GNode_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gmarkup_parse_context_autoptr: POINTER
               -- typedef GMarkupParseContext_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gmapped_file_autoptr: POINTER
               -- typedef GMappedFile_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gsource_autoptr: POINTER
               -- typedef GSource_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gmain_loop_autoptr: POINTER
               -- typedef GMainLoop_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gmain_context_autoptr: POINTER
               -- typedef GMainContext_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gbyte_array_autoptr: POINTER
               -- typedef GByteArray_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gptr_array_autoptr: POINTER
               -- typedef GPtrArray_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       garray_autoptr: POINTER
               -- typedef GArray_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       glist_autoptr: POINTER
               -- typedef GList_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gkey_file_autoptr: POINTER
               -- typedef GKeyFile_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       giochannel_autoptr: POINTER
               -- typedef GIOChannel_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       ghmac_autoptr: POINTER
               -- typedef GHmac_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       ghash_table_autoptr: POINTER
               -- typedef GHashTable_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gerror_autoptr: POINTER
               -- typedef GError_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gdir_autoptr: POINTER
               -- typedef GDir_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gdate_time_autoptr: POINTER
               -- typedef GDateTime_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gchecksum_autoptr: POINTER
               -- typedef GChecksum_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gbytes_autoptr: POINTER
               -- typedef GBytes_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gbookmark_file_autoptr: POINTER
               -- typedef GBookmarkFile_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gasync_queue_autoptr: POINTER
               -- typedef GAsyncQueue_autoptr
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gtraverse_func: POINTER
               -- typedef GTraverseFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gtest_log_fatal_func: POINTER
               -- typedef GTestLogFatalFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gtest_fixture_func: POINTER
               -- typedef GTestFixtureFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gtest_data_func: POINTER
               -- typedef GTestDataFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gtest_func: POINTER
               -- typedef GTestFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gstrv: POINTER
               -- typedef GStrv
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gspawn_child_setup_func: POINTER
               -- typedef GSpawnChildSetupFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gsequence_iter_compare_func: POINTER
               -- typedef GSequenceIterCompareFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gscanner_msg_func: POINTER
               -- typedef GScannerMsgFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gregex_eval_callback: POINTER
               -- typedef GRegexEvalCallback
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       goption_error_func: POINTER
               -- typedef GOptionErrorFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       goption_parse_func: POINTER
               -- typedef GOptionParseFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       goption_arg_func: POINTER
               -- typedef GOptionArgFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gprint_func: POINTER
               -- typedef GPrintFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       glog_func: POINTER
               -- typedef GLogFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       giofunc: POINTER
               -- typedef GIOFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gunichar2: NATURAL_16
               -- typedef gunichar2
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gunichar: NATURAL
               -- typedef gunichar
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gsource_dummy_marshal: POINTER
               -- typedef GSourceDummyMarshal
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gchild_watch_func: POINTER
               -- typedef GChildWatchFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gsource_func: POINTER
               -- typedef GSourceFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gpoll_func: POINTER
               -- typedef GPollFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       ghook_finalize_func: POINTER
               -- typedef GHookFinalizeFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       ghook_check_func: POINTER
               -- typedef GHookCheckFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       ghook_func: POINTER
               -- typedef GHookFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       ghook_check_marshaller: POINTER
               -- typedef GHookCheckMarshaller
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       ghook_marshaller: POINTER
               -- typedef GHookMarshaller
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       ghook_find_func: POINTER
               -- typedef GHookFindFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       ghook_compare_func: POINTER
               -- typedef GHookCompareFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       ghrfunc: POINTER
               -- typedef GHRFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gcopy_func: POINTER
               -- typedef GCopyFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gnode_foreach_func: POINTER
               -- typedef GNodeForeachFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gnode_traverse_func: POINTER
               -- typedef GNodeTraverseFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gtime_span: like long
               -- typedef GTimeSpan
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gdate_day: CHARACTER
               -- typedef GDateDay
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gdate_year: NATURAL_16
               -- typedef GDateYear
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gtime: INTEGER
               -- typedef GTime
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gduplicate_func: POINTER
               -- typedef GDuplicateFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gdata_foreach_func: POINTER
               -- typedef GDataForeachFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       giconv: POINTER
               -- typedef GIConv
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

	-- GMutexLocker unwrappable: no wrapper type.
       gthread_func: POINTER
               -- typedef GThreadFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gvoid_func: POINTER
               -- typedef GVoidFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gquark: NATURAL
               -- typedef GQuark
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gtranslate_func: POINTER
               -- typedef GTranslateFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gfree_func: POINTER
               -- typedef GFreeFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       ghfunc: POINTER
               -- typedef GHFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       ghash_func: POINTER
               -- typedef GHashFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gfunc: POINTER
               -- typedef GFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gdestroy_notify: POINTER
               -- typedef GDestroyNotify
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gequal_func: POINTER
               -- typedef GEqualFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gcompare_data_func: POINTER
               -- typedef GCompareDataFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gcompare_func: POINTER
               -- typedef GCompareFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gconstpointer: POINTER
               -- typedef gconstpointer
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gpointer: POINTER
               -- typedef gpointer
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gdouble: REAL
               -- typedef gdouble
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gfloat: REAL_32
               -- typedef gfloat
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       guint: NATURAL
               -- typedef guint
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gulong: like long_unsigned
               -- typedef gulong
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gushort: NATURAL_16
               -- typedef gushort
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       guchar: CHARACTER
               -- typedef guchar
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gboolean: INTEGER
               -- typedef gboolean
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gint: INTEGER
               -- typedef gint
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       glong: like long
               -- typedef glong
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gshort: INTEGER_16
               -- typedef gshort
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gchar: CHARACTER
               -- typedef gchar
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

end
