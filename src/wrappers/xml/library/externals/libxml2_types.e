-- This file have been created by wrapper-generator.
-- Any change will be lost by the next execution of the tool.

deferred class LIBXML2_TYPES


insert ANY undefine is_equal, copy end

	STANDARD_C_LIBRARY_TYPES

feature -- C type definitions (typedefs)
	xml_parser_input_buffer_ptr: POINTER is
		-- typedef xmlParserInputBufferPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_element_table_ptr: POINTER is
		-- typedef xmlElementTablePtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_error_ptr: POINTER is
		-- typedef xmlErrorPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	external_subset_saxfunc: POINTER is
		-- typedef externalSubsetSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_hash_deallocator: POINTER is
		-- typedef xmlHashDeallocator
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_list_ptr: POINTER is
		-- typedef xmlListPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_valid_ctxt_ptr: POINTER is
		-- typedef xmlValidCtxtPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_parser_input_ptr: POINTER is
		-- typedef xmlParserInputPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_input_open_callback: POINTER is
		-- typedef xmlInputOpenCallback
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	cdata_block_saxfunc: POINTER is
		-- typedef cdataBlockSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_automata_state_ptr: POINTER is
		-- typedef xmlAutomataStatePtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	unparsed_entity_decl_saxfunc: POINTER is
		-- typedef unparsedEntityDeclSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_char_encoding_input_func: POINTER is
		-- typedef xmlCharEncodingInputFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xlink_role: POINTER is
		-- typedef xlinkRole
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_hash_scanner: POINTER is
		-- typedef xmlHashScanner
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_validity_error_func: POINTER is
		-- typedef xmlValidityErrorFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_attr_ptr: POINTER is
		-- typedef xmlAttrPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	start_element_saxfunc: POINTER is
		-- typedef startElementSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	characters_saxfunc: POINTER is
		-- typedef charactersSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_valid_state_ptr: POINTER is
		-- typedef xmlValidStatePtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	comment_saxfunc: POINTER is
		-- typedef commentSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	start_document_saxfunc: POINTER is
		-- typedef startDocumentSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_external_entity_loader: POINTER is
		-- typedef xmlExternalEntityLoader
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_reg_exec_ctxt_ptr: POINTER is
		-- typedef xmlRegExecCtxtPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ignorable_whitespace_saxfunc: POINTER is
		-- typedef ignorableWhitespaceSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_input_match_callback: POINTER is
		-- typedef xmlInputMatchCallback
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_exp_ctxt_ptr: POINTER is
		-- typedef xmlExpCtxtPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xlink_title: POINTER is
		-- typedef xlinkTitle
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_deregister_node_func: POINTER is
		-- typedef xmlDeregisterNodeFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_saxlocator_ptr: POINTER is
		-- typedef xmlSAXLocatorPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_attribute_ptr: POINTER is
		-- typedef xmlAttributePtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_domwrap_ctxt_ptr: POINTER is
		-- typedef xmlDOMWrapCtxtPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_validity_warning_func: POINTER is
		-- typedef xmlValidityWarningFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_element_ptr: POINTER is
		-- typedef xmlElementPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_free_func: POINTER is
		-- typedef xmlFreeFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	has_internal_subset_saxfunc: POINTER is
		-- typedef hasInternalSubsetSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	element_decl_saxfunc: POINTER is
		-- typedef elementDeclSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_parser_input_buffer_create_filename_func: POINTER is
		-- typedef xmlParserInputBufferCreateFilenameFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_output_close_callback: POINTER is
		-- typedef xmlOutputCloseCallback
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_ref_table_ptr: POINTER is
		-- typedef xmlRefTablePtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	fatal_error_saxfunc: POINTER is
		-- typedef fatalErrorSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_generic_error_func: POINTER is
		-- typedef xmlGenericErrorFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	get_entity_saxfunc: POINTER is
		-- typedef getEntitySAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_parser_node_info_ptr: POINTER is
		-- typedef xmlParserNodeInfoPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_output_buffer_ptr: POINTER is
		-- typedef xmlOutputBufferPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	end_document_saxfunc: POINTER is
		-- typedef endDocumentSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_notation_table_ptr: POINTER is
		-- typedef xmlNotationTablePtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_parser_ctxt_ptr: POINTER is
		-- typedef xmlParserCtxtPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_ns_ptr: POINTER is
		-- typedef xmlNsPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_output_open_callback: POINTER is
		-- typedef xmlOutputOpenCallback
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xlink_extended_link_funk: POINTER is
		-- typedef xlinkExtendedLinkFunk
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_char_encoding_handler_ptr: POINTER is
		-- typedef xmlCharEncodingHandlerPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_automata_ptr: POINTER is
		-- typedef xmlAutomataPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_structured_error_func: POINTER is
		-- typedef xmlStructuredErrorFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_element_content_ptr: POINTER is
		-- typedef xmlElementContentPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_list_walker: POINTER is
		-- typedef xmlListWalker
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_notation_ptr: POINTER is
		-- typedef xmlNotationPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_char: CHARACTER is
		-- typedef xmlChar
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_entities_table_ptr: POINTER is
		-- typedef xmlEntitiesTablePtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	notation_decl_saxfunc: POINTER is
		-- typedef notationDeclSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_ref_ptr: POINTER is
		-- typedef xmlRefPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_exp_node_ptr: POINTER is
		-- typedef xmlExpNodePtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xlink_simple_link_funk: POINTER is
		-- typedef xlinkSimpleLinkFunk
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	entity_decl_saxfunc: POINTER is
		-- typedef entityDeclSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_hash_table_ptr: POINTER is
		-- typedef xmlHashTablePtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_domwrap_acquire_ns_function: POINTER is
		-- typedef xmlDOMWrapAcquireNsFunction
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xlink_handler_ptr: POINTER is
		-- typedef xlinkHandlerPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_char_encoding_output_func: POINTER is
		-- typedef xmlCharEncodingOutputFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_output_buffer_create_filename_func: POINTER is
		-- typedef xmlOutputBufferCreateFilenameFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	end_element_saxfunc: POINTER is
		-- typedef endElementSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	internal_subset_saxfunc: POINTER is
		-- typedef internalSubsetSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	warning_saxfunc: POINTER is
		-- typedef warningSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_strdup_func: POINTER is
		-- typedef xmlStrdupFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	attribute_decl_saxfunc: POINTER is
		-- typedef attributeDeclSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_enumeration_ptr: POINTER is
		-- typedef xmlEnumerationPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_realloc_func: POINTER is
		-- typedef xmlReallocFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_saxhandler_ptr: POINTER is
		-- typedef xmlSAXHandlerPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_saxhandler_v1ptr: POINTER is
		-- typedef xmlSAXHandlerV1Ptr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	has_external_subset_saxfunc: POINTER is
		-- typedef hasExternalSubsetSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	reference_saxfunc: POINTER is
		-- typedef referenceSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_node_ptr: POINTER is
		-- typedef xmlNodePtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	get_parameter_entity_saxfunc: POINTER is
		-- typedef getParameterEntitySAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_register_node_func: POINTER is
		-- typedef xmlRegisterNodeFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	set_document_locator_saxfunc: POINTER is
		-- typedef setDocumentLocatorSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_doc_ptr: POINTER is
		-- typedef xmlDocPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_dtd_ptr: POINTER is
		-- typedef xmlDtdPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_regexp_ptr: POINTER is
		-- typedef xmlRegexpPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	error_saxfunc: POINTER is
		-- typedef errorSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_hash_scanner_full: POINTER is
		-- typedef xmlHashScannerFull
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	is_standalone_saxfunc: POINTER is
		-- typedef isStandaloneSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_dict_ptr: POINTER is
		-- typedef xmlDictPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_rmutex_ptr: POINTER is
		-- typedef xmlRMutexPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_entity_ptr: POINTER is
		-- typedef xmlEntityPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_output_write_callback: POINTER is
		-- typedef xmlOutputWriteCallback
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	resolve_entity_saxfunc: POINTER is
		-- typedef resolveEntitySAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_output_match_callback: POINTER is
		-- typedef xmlOutputMatchCallback
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xlink_extended_link_set_funk: POINTER is
		-- typedef xlinkExtendedLinkSetFunk
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xlink_href: POINTER is
		-- typedef xlinkHRef
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_parser_node_info_seq_ptr: POINTER is
		-- typedef xmlParserNodeInfoSeqPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_list_data_compare: POINTER is
		-- typedef xmlListDataCompare
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_reg_exec_callbacks: POINTER is
		-- typedef xmlRegExecCallbacks
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xlink_node_detect_func: POINTER is
		-- typedef xlinkNodeDetectFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_link_ptr: POINTER is
		-- typedef xmlLinkPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	attribute_saxfunc: POINTER is
		-- typedef attributeSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_parser_input_deallocate: POINTER is
		-- typedef xmlParserInputDeallocate
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_idtable_ptr: POINTER is
		-- typedef xmlIDTablePtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_global_state_ptr: POINTER is
		-- typedef xmlGlobalStatePtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_buffer_ptr: POINTER is
		-- typedef xmlBufferPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_input_read_callback: POINTER is
		-- typedef xmlInputReadCallback
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_malloc_func: POINTER is
		-- typedef xmlMallocFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_list_deallocator: POINTER is
		-- typedef xmlListDeallocator
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_input_close_callback: POINTER is
		-- typedef xmlInputCloseCallback
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_idptr: POINTER is
		-- typedef xmlIDPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_hash_copier: POINTER is
		-- typedef xmlHashCopier
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	start_element_ns_sax2func: POINTER is
		-- typedef startElementNsSAX2Func
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	end_element_ns_sax2func: POINTER is
		-- typedef endElementNsSAX2Func
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_mutex_ptr: POINTER is
		-- typedef xmlMutexPtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	processing_instruction_saxfunc: POINTER is
		-- typedef processingInstructionSAXFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	xml_attribute_table_ptr: POINTER is
		-- typedef xmlAttributeTablePtr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

end
