-- This file have been created by wrapper-generator.
-- Any change will be lost by the next execution of the tool.
deferred class GOBJECT_TYPES


insert ANY undefine is_equal, copy end

	STANDARD_C_LIBRARY_TYPES

feature {ANY} -- C type definitions (typedefs)
       gboxed_free_func: POINTER
               -- typedef GBoxedFreeFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       ginstance_init_func: POINTER
               -- typedef GInstanceInitFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gobject_set_property_func: POINTER
               -- typedef GObjectSetPropertyFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gtype: like long_unsigned
               -- typedef GType
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gsignal_cmarshaller: POINTER
               -- typedef GSignalCMarshaller
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gbase_finalize_func: POINTER
               -- typedef GBaseFinalizeFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gboxed_copy_func: POINTER
               -- typedef GBoxedCopyFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gweak_notify: POINTER
               -- typedef GWeakNotify
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       ginterface_finalize_func: POINTER
               -- typedef GInterfaceFinalizeFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gclosure_notify: POINTER
               -- typedef GClosureNotify
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gbinding_transform_func: POINTER
               -- typedef GBindingTransformFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gsignal_cva_marshaller: POINTER
               -- typedef GSignalCVaMarshaller
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gchararray: POINTER
               -- typedef gchararray
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gtype_plugin_unuse: POINTER
               -- typedef GTypePluginUnuse
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gtype_interface_check_func: POINTER
               -- typedef GTypeInterfaceCheckFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gva_closure_marshal: POINTER
               -- typedef GVaClosureMarshal
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gtype_class_cache_func: POINTER
               -- typedef GTypeClassCacheFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gsignal_emission_hook: POINTER
               -- typedef GSignalEmissionHook
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gcallback: POINTER
               -- typedef GCallback
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gclass_finalize_func: POINTER
               -- typedef GClassFinalizeFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gtoggle_notify: POINTER
               -- typedef GToggleNotify
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gstrv: POINTER
               -- typedef GStrv
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gtype_plugin_use: POINTER
               -- typedef GTypePluginUse
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gobject_finalize_func: POINTER
               -- typedef GObjectFinalizeFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gclass_init_func: POINTER
               -- typedef GClassInitFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       ginterface_init_func: POINTER
               -- typedef GInterfaceInitFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gsignal_accumulator: POINTER
               -- typedef GSignalAccumulator
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gclosure_marshal: POINTER
               -- typedef GClosureMarshal
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gobject_get_property_func: POINTER
               -- typedef GObjectGetPropertyFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gbase_init_func: POINTER
               -- typedef GBaseInitFunc
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gtype_plugin_complete_type_info: POINTER
               -- typedef GTypePluginCompleteTypeInfo
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gvalue_transform: POINTER
               -- typedef GValueTransform
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       gtype_plugin_complete_interface_info: POINTER
               -- typedef GTypePluginCompleteInterfaceInfo
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

end
