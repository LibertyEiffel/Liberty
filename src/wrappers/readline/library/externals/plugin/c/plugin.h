/*
** This file have been created by wrapper-generator.
** Any change will be lost by the next execution of the tool.
*/
#define k_eymap_entry_struct_get_type(a_structure) (((_keymap_entry*) a_structure).type)

#define k_eymap_entry_struct_set_type(a_structure,a_value) ((_keymap_entry)->type = a_value;

#define k_eymap_entry_struct_get_function(a_structure) (((_keymap_entry*) a_structure).function)

#define k_eymap_entry_struct_set_function(a_structure,a_value) ((_keymap_entry)->function = a_value;

#define f_unmap_struct_get_name(a_structure) (((_funmap*) a_structure).name)

#define f_unmap_struct_set_name(a_structure,a_value) ((_funmap)->name = a_value;

#define f_unmap_struct_get_function(a_structure) (((_funmap*) a_structure).function)

#define f_unmap_struct_set_function(a_structure,a_value) ((_funmap)->function = a_value;

#define h_ist_entry_struct_get_line(a_structure) (((_hist_entry*) a_structure).line)

#define h_ist_entry_struct_set_line(a_structure,a_value) ((_hist_entry)->line = a_value;

#define h_ist_entry_struct_get_timestamp(a_structure) (((_hist_entry*) a_structure).timestamp)

#define h_ist_entry_struct_set_timestamp(a_structure,a_value) ((_hist_entry)->timestamp = a_value;

#define h_ist_entry_struct_get_data(a_structure) (((_hist_entry*) a_structure).data)

#define h_ist_entry_struct_set_data(a_structure,a_value) ((_hist_entry)->data = a_value;

#define readline_state_struct_get_point(a_structure) (((readline_state*) a_structure).point)

#define readline_state_struct_set_point(a_structure,a_value) ((readline_state)->point = a_value;

#define readline_state_struct_get_end_external(a_structure) (((readline_state*) a_structure).end)

#define readline_state_struct_set_end_external(a_structure,a_value) ((readline_state)->end = a_value;

#define readline_state_struct_get_mark(a_structure) (((readline_state*) a_structure).mark)

#define readline_state_struct_set_mark(a_structure,a_value) ((readline_state)->mark = a_value;

#define readline_state_struct_get_buffer(a_structure) (((readline_state*) a_structure).buffer)

#define readline_state_struct_set_buffer(a_structure,a_value) ((readline_state)->buffer = a_value;

#define readline_state_struct_get_buflen(a_structure) (((readline_state*) a_structure).buflen)

#define readline_state_struct_set_buflen(a_structure,a_value) ((readline_state)->buflen = a_value;

#define readline_state_struct_get_ul(a_structure) (((readline_state*) a_structure).ul)

#define readline_state_struct_set_ul(a_structure,a_value) ((readline_state)->ul = a_value;

#define readline_state_struct_get_prompt(a_structure) (((readline_state*) a_structure).prompt)

#define readline_state_struct_set_prompt(a_structure,a_value) ((readline_state)->prompt = a_value;

#define readline_state_struct_get_rlstate(a_structure) (((readline_state*) a_structure).rlstate)

#define readline_state_struct_set_rlstate(a_structure,a_value) ((readline_state)->rlstate = a_value;

#define readline_state_struct_get_done(a_structure) (((readline_state*) a_structure).done)

#define readline_state_struct_set_done(a_structure,a_value) ((readline_state)->done = a_value;

#define readline_state_struct_get_kmap(a_structure) (((readline_state*) a_structure).kmap)

#define readline_state_struct_set_kmap(a_structure,a_value) ((readline_state)->kmap = a_value;

#define readline_state_struct_get_lastfunc(a_structure) (((readline_state*) a_structure).lastfunc)

#define readline_state_struct_set_lastfunc(a_structure,a_value) ((readline_state)->lastfunc = a_value;

#define readline_state_struct_get_insmode(a_structure) (((readline_state*) a_structure).insmode)

#define readline_state_struct_set_insmode(a_structure,a_value) ((readline_state)->insmode = a_value;

#define readline_state_struct_get_edmode(a_structure) (((readline_state*) a_structure).edmode)

#define readline_state_struct_set_edmode(a_structure,a_value) ((readline_state)->edmode = a_value;

#define readline_state_struct_get_kseqlen(a_structure) (((readline_state*) a_structure).kseqlen)

#define readline_state_struct_set_kseqlen(a_structure,a_value) ((readline_state)->kseqlen = a_value;

#define readline_state_struct_get_inf(a_structure) (((readline_state*) a_structure).inf)

#define readline_state_struct_set_inf(a_structure,a_value) ((readline_state)->inf = a_value;

#define readline_state_struct_get_outf(a_structure) (((readline_state*) a_structure).outf)

#define readline_state_struct_set_outf(a_structure,a_value) ((readline_state)->outf = a_value;

#define readline_state_struct_get_pendingin(a_structure) (((readline_state*) a_structure).pendingin)

#define readline_state_struct_set_pendingin(a_structure,a_value) ((readline_state)->pendingin = a_value;

#define readline_state_struct_get_macro(a_structure) (((readline_state*) a_structure).macro)

#define readline_state_struct_set_macro(a_structure,a_value) ((readline_state)->macro = a_value;

#define readline_state_struct_get_catchsigs(a_structure) (((readline_state*) a_structure).catchsigs)

#define readline_state_struct_set_catchsigs(a_structure,a_value) ((readline_state)->catchsigs = a_value;

#define readline_state_struct_get_catchsigwinch(a_structure) (((readline_state*) a_structure).catchsigwinch)

#define readline_state_struct_set_catchsigwinch(a_structure,a_value) ((readline_state)->catchsigwinch = a_value;

#define readline_state_struct_get_reserved(a_structure) (((readline_state*) a_structure).reserved)

#define readline_state_struct_set_reserved(a_structure,a_value) ((readline_state)->reserved = a_value;

#define u_ndo_list_struct_get_next(a_structure) (((undo_list*) a_structure).next)

#define u_ndo_list_struct_set_next(a_structure,a_value) ((undo_list)->next = a_value;

#define u_ndo_list_struct_get_start(a_structure) (((undo_list*) a_structure).start)

#define u_ndo_list_struct_set_start(a_structure,a_value) ((undo_list)->start = a_value;

#define u_ndo_list_struct_get_end_external(a_structure) (((undo_list*) a_structure).end)

#define u_ndo_list_struct_set_end_external(a_structure,a_value) ((undo_list)->end = a_value;

#define u_ndo_list_struct_get_text(a_structure) (((undo_list*) a_structure).text)

#define u_ndo_list_struct_set_text(a_structure,a_value) ((undo_list)->text = a_value;

#define u_ndo_list_struct_get_what(a_structure) (((undo_list*) a_structure).what)

#define u_ndo_list_struct_set_what(a_structure,a_value) ((undo_list)->what = a_value;

#define h_istory_state_struct_get_entries(a_structure) (((_hist_state*) a_structure).entries)

#define h_istory_state_struct_set_entries(a_structure,a_value) ((_hist_state)->entries = a_value;

#define h_istory_state_struct_get_offset(a_structure) (((_hist_state*) a_structure).offset)

#define h_istory_state_struct_set_offset(a_structure,a_value) ((_hist_state)->offset = a_value;

#define h_istory_state_struct_get_length(a_structure) (((_hist_state*) a_structure).length)

#define h_istory_state_struct_set_length(a_structure,a_value) ((_hist_state)->length = a_value;

#define h_istory_state_struct_get_size(a_structure) (((_hist_state*) a_structure).size)

#define h_istory_state_struct_set_size(a_structure,a_value) ((_hist_state)->size = a_value;

#define h_istory_state_struct_get_flags(a_structure) (((_hist_state*) a_structure).flags)

#define h_istory_state_struct_set_flags(a_structure,a_value) ((_hist_state)->flags = a_value;

