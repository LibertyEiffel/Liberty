/*
** This file have been created by wrapper-generator.
** Any change will be lost by the next execution of the tool.
*/
#define keymap_entry_struct_get_type(a_structure) (((struct _keymap_entry*) (a_structure))->type)

#define keymap_entry_struct_set_type(a_structure,a_value) (((struct _keymap_entry*)(a_structur)e)))->type = a_value;

#define keymap_entry_struct_get_function(a_structure) (((struct _keymap_entry*) (a_structure))->function)

#define keymap_entry_struct_set_function(a_structure,a_value) (((struct _keymap_entry*)(a_structur)e)))->function = a_value;

#define funmap_struct_get_name(a_structure) (((struct _funmap*) (a_structure))->name)

#define funmap_struct_set_name(a_structure,a_value) (((struct _funmap*)(a_structur)e)))->name = a_value;

#define funmap_struct_get_function(a_structure) (((struct _funmap*) (a_structure))->function)

#define funmap_struct_set_function(a_structure,a_value) (((struct _funmap*)(a_structur)e)))->function = a_value;

#define hist_entry_struct_get_line(a_structure) (((struct _hist_entry*) (a_structure))->line)

#define hist_entry_struct_set_line(a_structure,a_value) (((struct _hist_entry*)(a_structur)e)))->line = a_value;

#define hist_entry_struct_get_timestamp(a_structure) (((struct _hist_entry*) (a_structure))->timestamp)

#define hist_entry_struct_set_timestamp(a_structure,a_value) (((struct _hist_entry*)(a_structur)e)))->timestamp = a_value;

#define hist_entry_struct_get_data(a_structure) (((struct _hist_entry*) (a_structure))->data)

#define hist_entry_struct_set_data(a_structure,a_value) (((struct _hist_entry*)(a_structur)e)))->data = a_value;

#define readline_state_struct_get_point(a_structure) (((struct readline_state*) (a_structure))->point)

#define readline_state_struct_set_point(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->point = a_value;

#define readline_state_struct_get_end_external(a_structure) (((struct readline_state*) (a_structure))->end)

#define readline_state_struct_set_end_external(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->end = a_value;

#define readline_state_struct_get_mark(a_structure) (((struct readline_state*) (a_structure))->mark)

#define readline_state_struct_set_mark(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->mark = a_value;

#define readline_state_struct_get_buffer(a_structure) (((struct readline_state*) (a_structure))->buffer)

#define readline_state_struct_set_buffer(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->buffer = a_value;

#define readline_state_struct_get_buflen(a_structure) (((struct readline_state*) (a_structure))->buflen)

#define readline_state_struct_set_buflen(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->buflen = a_value;

#define readline_state_struct_get_ul(a_structure) (((struct readline_state*) (a_structure))->ul)

#define readline_state_struct_set_ul(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->ul = a_value;

#define readline_state_struct_get_prompt(a_structure) (((struct readline_state*) (a_structure))->prompt)

#define readline_state_struct_set_prompt(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->prompt = a_value;

#define readline_state_struct_get_rlstate(a_structure) (((struct readline_state*) (a_structure))->rlstate)

#define readline_state_struct_set_rlstate(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->rlstate = a_value;

#define readline_state_struct_get_done(a_structure) (((struct readline_state*) (a_structure))->done)

#define readline_state_struct_set_done(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->done = a_value;

#define readline_state_struct_get_kmap(a_structure) (((struct readline_state*) (a_structure))->kmap)

#define readline_state_struct_set_kmap(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->kmap = a_value;

#define readline_state_struct_get_lastfunc(a_structure) (((struct readline_state*) (a_structure))->lastfunc)

#define readline_state_struct_set_lastfunc(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->lastfunc = a_value;

#define readline_state_struct_get_insmode(a_structure) (((struct readline_state*) (a_structure))->insmode)

#define readline_state_struct_set_insmode(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->insmode = a_value;

#define readline_state_struct_get_edmode(a_structure) (((struct readline_state*) (a_structure))->edmode)

#define readline_state_struct_set_edmode(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->edmode = a_value;

#define readline_state_struct_get_kseqlen(a_structure) (((struct readline_state*) (a_structure))->kseqlen)

#define readline_state_struct_set_kseqlen(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->kseqlen = a_value;

#define readline_state_struct_get_inf(a_structure) (((struct readline_state*) (a_structure))->inf)

#define readline_state_struct_set_inf(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->inf = a_value;

#define readline_state_struct_get_outf(a_structure) (((struct readline_state*) (a_structure))->outf)

#define readline_state_struct_set_outf(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->outf = a_value;

#define readline_state_struct_get_pendingin(a_structure) (((struct readline_state*) (a_structure))->pendingin)

#define readline_state_struct_set_pendingin(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->pendingin = a_value;

#define readline_state_struct_get_macro(a_structure) (((struct readline_state*) (a_structure))->macro)

#define readline_state_struct_set_macro(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->macro = a_value;

#define readline_state_struct_get_catchsigs(a_structure) (((struct readline_state*) (a_structure))->catchsigs)

#define readline_state_struct_set_catchsigs(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->catchsigs = a_value;

#define readline_state_struct_get_catchsigwinch(a_structure) (((struct readline_state*) (a_structure))->catchsigwinch)

#define readline_state_struct_set_catchsigwinch(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->catchsigwinch = a_value;

#define readline_state_struct_get_reserved(a_structure) (((struct readline_state*) (a_structure))->reserved)

#define readline_state_struct_set_reserved(a_structure,a_value) (((struct readline_state*)(a_structur)e)))->reserved = a_value;

#define undo_list_struct_get_next(a_structure) (((struct undo_list*) (a_structure))->next)

#define undo_list_struct_set_next(a_structure,a_value) (((struct undo_list*)(a_structur)e)))->next = a_value;

#define undo_list_struct_get_start(a_structure) (((struct undo_list*) (a_structure))->start)

#define undo_list_struct_set_start(a_structure,a_value) (((struct undo_list*)(a_structur)e)))->start = a_value;

#define undo_list_struct_get_end_external(a_structure) (((struct undo_list*) (a_structure))->end)

#define undo_list_struct_set_end_external(a_structure,a_value) (((struct undo_list*)(a_structur)e)))->end = a_value;

#define undo_list_struct_get_text(a_structure) (((struct undo_list*) (a_structure))->text)

#define undo_list_struct_set_text(a_structure,a_value) (((struct undo_list*)(a_structur)e)))->text = a_value;

#define undo_list_struct_get_what(a_structure) (((struct undo_list*) (a_structure))->what)

#define undo_list_struct_set_what(a_structure,a_value) (((struct undo_list*)(a_structur)e)))->what = a_value;

#define history_state_struct_get_entries(a_structure) (((struct _hist_state*) (a_structure))->entries)

#define history_state_struct_set_entries(a_structure,a_value) (((struct _hist_state*)(a_structur)e)))->entries = a_value;

#define history_state_struct_get_offset(a_structure) (((struct _hist_state*) (a_structure))->offset)

#define history_state_struct_set_offset(a_structure,a_value) (((struct _hist_state*)(a_structur)e)))->offset = a_value;

#define history_state_struct_get_length(a_structure) (((struct _hist_state*) (a_structure))->length)

#define history_state_struct_set_length(a_structure,a_value) (((struct _hist_state*)(a_structur)e)))->length = a_value;

#define history_state_struct_get_size(a_structure) (((struct _hist_state*) (a_structure))->size)

#define history_state_struct_set_size(a_structure,a_value) (((struct _hist_state*)(a_structur)e)))->size = a_value;

#define history_state_struct_get_flags(a_structure) (((struct _hist_state*) (a_structure))->flags)

#define history_state_struct_set_flags(a_structure,a_value) (((struct _hist_state*)(a_structur)e)))->flags = a_value;

