-- This file have been created by wrapper-generator.
-- Any change will be lost by the next execution of the tool.

deferred class GSL_TYPES


insert ANY undefine is_equal, copy end

	STANDARD_C_LIBRARY_TYPES

feature -- C type definitions (typedefs)
	gsl_complex_packed_array_float: POINTER is
		-- typedef gsl_complex_packed_array_float
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_siman_metric_t: POINTER is
		-- typedef gsl_siman_metric_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_siman_print_t: POINTER is
		-- typedef gsl_siman_print_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_siman_step_t: POINTER is
		-- typedef gsl_siman_step_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_complex_packed_float: POINTER is
		-- typedef gsl_complex_packed_float
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_const_complex_packed_long_double: POINTER is
		-- typedef gsl_const_complex_packed_long_double
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_mode_t: NATURAL is
		-- typedef gsl_mode_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_const_complex_packed_float: POINTER is
		-- typedef gsl_const_complex_packed_float
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_const_complex_packed_array_float: POINTER is
		-- typedef gsl_const_complex_packed_array_float
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_complex_packed_ptr: POINTER is
		-- typedef gsl_complex_packed_ptr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_complex_packed_long_double_ptr: POINTER is
		-- typedef gsl_complex_packed_long_double_ptr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_const_complex_packed_float_ptr: POINTER is
		-- typedef gsl_const_complex_packed_float_ptr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_prec_t: NATURAL is
		-- typedef gsl_prec_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_complex_packed: POINTER is
		-- typedef gsl_complex_packed
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_siman_copy_construct_t: POINTER is
		-- typedef gsl_siman_copy_construct_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_min_bracketing_function: POINTER is
		-- typedef gsl_min_bracketing_function
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_const_complex_packed_long_double_ptr: POINTER is
		-- typedef gsl_const_complex_packed_long_double_ptr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_complex_packed_long_double: POINTER is
		-- typedef gsl_complex_packed_long_double
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_const_complex_packed_array: POINTER is
		-- typedef gsl_const_complex_packed_array
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_complex_packed_float_ptr: POINTER is
		-- typedef gsl_complex_packed_float_ptr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_complex_packed_array_long_double: POINTER is
		-- typedef gsl_complex_packed_array_long_double
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_siman_destroy_t: POINTER is
		-- typedef gsl_siman_destroy_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_const_complex_packed: POINTER is
		-- typedef gsl_const_complex_packed
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_comparison_fn_t: POINTER is
		-- typedef gsl_comparison_fn_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_const_complex_packed_ptr: POINTER is
		-- typedef gsl_const_complex_packed_ptr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_const_complex_packed_array_long_double: POINTER is
		-- typedef gsl_const_complex_packed_array_long_double
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_complex_packed_array: POINTER is
		-- typedef gsl_complex_packed_array
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_siman_efunc_t: POINTER is
		-- typedef gsl_siman_Efunc_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsl_siman_copy_t: POINTER is
		-- typedef gsl_siman_copy_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	cblas_index_t: like size_t is
		-- typedef CBLAS_INDEX_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

end
