/*
** This file have been created by wrapper-generator.
** Any change will be lost by the next execution of the tool.
*/
#define sizeof_cl_target_option (sizeof(struct cl_target_option))
#define sizeof_rtx_def (sizeof(struct rtx_def))
#define sizeof_cpp_token (sizeof(struct cpp_token))
#define sizeof_cl_decoded_option (sizeof(struct cl_decoded_option))
#define plugin_gcc_version_struct_get_basever(a_structure) (((struct plugin_gcc_version*) (a_structure))->basever)

#define plugin_gcc_version_struct_set_basever(a_structure,a_value) do {(((struct plugin_gcc_version*)(a_structure)))->basever = (a_value);}while(0)

#define plugin_gcc_version_struct_get_datestamp(a_structure) (((struct plugin_gcc_version*) (a_structure))->datestamp)

#define plugin_gcc_version_struct_set_datestamp(a_structure,a_value) do {(((struct plugin_gcc_version*)(a_structure)))->datestamp = (a_value);}while(0)

#define plugin_gcc_version_struct_get_devphase(a_structure) (((struct plugin_gcc_version*) (a_structure))->devphase)

#define plugin_gcc_version_struct_set_devphase(a_structure,a_value) do {(((struct plugin_gcc_version*)(a_structure)))->devphase = (a_value);}while(0)

#define plugin_gcc_version_struct_get_revision(a_structure) (((struct plugin_gcc_version*) (a_structure))->revision)

#define plugin_gcc_version_struct_set_revision(a_structure,a_value) do {(((struct plugin_gcc_version*)(a_structure)))->revision = (a_value);}while(0)

#define plugin_gcc_version_struct_get_configuration_arguments(a_structure) (((struct plugin_gcc_version*) (a_structure))->configuration_arguments)

#define plugin_gcc_version_struct_set_configuration_arguments(a_structure,a_value) do {(((struct plugin_gcc_version*)(a_structure)))->configuration_arguments = (a_value);}while(0)

#define sizeof_plugin_gcc_version (sizeof(struct plugin_gcc_version))
#define sizeof_diagnostic_context (sizeof(struct diagnostic_context))
#define sizeof_gimple_statement_base (sizeof(struct gimple_statement_base))
#define sizeof_cpp_reader (sizeof(struct cpp_reader))
#define sizeof_bitmap_head (sizeof(struct bitmap_head))
#define sizeof_pretty_printer (sizeof(struct pretty_printer))
#define sizeof_edge_def (sizeof(struct edge_def))
#define sizeof_cl_option (sizeof(struct cl_option))
#define sizeof_rtvec_def (sizeof(struct rtvec_def))
#define htab_struct_get_hash_f(a_structure) (((struct htab*) (a_structure))->hash_f)

#define htab_struct_set_hash_f(a_structure,a_value) do {(((struct htab*)(a_structure)))->hash_f = (a_value);}while(0)

#define htab_struct_get_eq_f(a_structure) (((struct htab*) (a_structure))->eq_f)

#define htab_struct_set_eq_f(a_structure,a_value) do {(((struct htab*)(a_structure)))->eq_f = (a_value);}while(0)

#define htab_struct_get_del_f(a_structure) (((struct htab*) (a_structure))->del_f)

#define htab_struct_set_del_f(a_structure,a_value) do {(((struct htab*)(a_structure)))->del_f = (a_value);}while(0)

#define htab_struct_get_entries(a_structure) (((struct htab*) (a_structure))->entries)

#define htab_struct_set_entries(a_structure,a_value) do {(((struct htab*)(a_structure)))->entries = (a_value);}while(0)

#define htab_struct_get_size(a_structure) (((struct htab*) (a_structure))->size)

#define htab_struct_set_size(a_structure,a_value) do {(((struct htab*)(a_structure)))->size = (a_value);}while(0)

#define htab_struct_get_n_elements(a_structure) (((struct htab*) (a_structure))->n_elements)

#define htab_struct_set_n_elements(a_structure,a_value) do {(((struct htab*)(a_structure)))->n_elements = (a_value);}while(0)

#define htab_struct_get_n_deleted(a_structure) (((struct htab*) (a_structure))->n_deleted)

#define htab_struct_set_n_deleted(a_structure,a_value) do {(((struct htab*)(a_structure)))->n_deleted = (a_value);}while(0)

#define htab_struct_get_searches(a_structure) (((struct htab*) (a_structure))->searches)

#define htab_struct_set_searches(a_structure,a_value) do {(((struct htab*)(a_structure)))->searches = (a_value);}while(0)

#define htab_struct_get_collisions(a_structure) (((struct htab*) (a_structure))->collisions)

#define htab_struct_set_collisions(a_structure,a_value) do {(((struct htab*)(a_structure)))->collisions = (a_value);}while(0)

#define htab_struct_get_alloc_f(a_structure) (((struct htab*) (a_structure))->alloc_f)

#define htab_struct_set_alloc_f(a_structure,a_value) do {(((struct htab*)(a_structure)))->alloc_f = (a_value);}while(0)

#define htab_struct_get_free_f(a_structure) (((struct htab*) (a_structure))->free_f)

#define htab_struct_set_free_f(a_structure,a_value) do {(((struct htab*)(a_structure)))->free_f = (a_value);}while(0)

#define htab_struct_get_alloc_arg(a_structure) (((struct htab*) (a_structure))->alloc_arg)

#define htab_struct_set_alloc_arg(a_structure,a_value) do {(((struct htab*)(a_structure)))->alloc_arg = (a_value);}while(0)

#define htab_struct_get_alloc_with_arg_f(a_structure) (((struct htab*) (a_structure))->alloc_with_arg_f)

#define htab_struct_set_alloc_with_arg_f(a_structure,a_value) do {(((struct htab*)(a_structure)))->alloc_with_arg_f = (a_value);}while(0)

#define htab_struct_get_free_with_arg_f(a_structure) (((struct htab*) (a_structure))->free_with_arg_f)

#define htab_struct_set_free_with_arg_f(a_structure,a_value) do {(((struct htab*)(a_structure)))->free_with_arg_f = (a_value);}while(0)

#define htab_struct_get_size_prime_index(a_structure) (((struct htab*) (a_structure))->size_prime_index)

#define htab_struct_set_size_prime_index(a_structure,a_value) do {(((struct htab*)(a_structure)))->size_prime_index = (a_value);}while(0)

#define sizeof_htab (sizeof(struct htab))
#define sizeof_gimple_stmt_iterator (sizeof(struct gimple_stmt_iterator))
#define plugin_name_args_struct_get_base_name(a_structure) (((struct plugin_name_args*) (a_structure))->base_name)

#define plugin_name_args_struct_set_base_name(a_structure,a_value) do {(((struct plugin_name_args*)(a_structure)))->base_name = (a_value);}while(0)

#define plugin_name_args_struct_get_full_name(a_structure) (((struct plugin_name_args*) (a_structure))->full_name)

#define plugin_name_args_struct_set_full_name(a_structure,a_value) do {(((struct plugin_name_args*)(a_structure)))->full_name = (a_value);}while(0)

#define plugin_name_args_struct_get_argc(a_structure) (((struct plugin_name_args*) (a_structure))->argc)

#define plugin_name_args_struct_set_argc(a_structure,a_value) do {(((struct plugin_name_args*)(a_structure)))->argc = (a_value);}while(0)

#define plugin_name_args_struct_get_argv(a_structure) (((struct plugin_name_args*) (a_structure))->argv)

#define plugin_name_args_struct_set_argv(a_structure,a_value) do {(((struct plugin_name_args*)(a_structure)))->argv = (a_value);}while(0)

#define plugin_name_args_struct_get_version(a_structure) (((struct plugin_name_args*) (a_structure))->version)

#define plugin_name_args_struct_set_version(a_structure,a_value) do {(((struct plugin_name_args*)(a_structure)))->version = (a_value);}while(0)

#define plugin_name_args_struct_get_help(a_structure) (((struct plugin_name_args*) (a_structure))->help)

#define plugin_name_args_struct_set_help(a_structure,a_value) do {(((struct plugin_name_args*)(a_structure)))->help = (a_value);}while(0)

#define sizeof_plugin_name_args (sizeof(struct plugin_name_args))
#define plugin_argument_struct_get_key(a_structure) (((struct plugin_argument*) (a_structure))->key)

#define plugin_argument_struct_set_key(a_structure,a_value) do {(((struct plugin_argument*)(a_structure)))->key = (a_value);}while(0)

#define plugin_argument_struct_get_value(a_structure) (((struct plugin_argument*) (a_structure))->value)

#define plugin_argument_struct_set_value(a_structure,a_value) do {(((struct plugin_argument*)(a_structure)))->value = (a_value);}while(0)

#define sizeof_plugin_argument (sizeof(struct plugin_argument))
#define sizeof_rtl_opt_pass (sizeof(struct rtl_opt_pass))
#define sizeof_simple_bitmap_def (sizeof(struct simple_bitmap_def))
#define sizeof_basic_block_def (sizeof(struct basic_block_def))
#define pex_time_struct_get_user_seconds(a_structure) (((struct pex_time*) (a_structure))->user_seconds)

#define pex_time_struct_set_user_seconds(a_structure,a_value) do {(((struct pex_time*)(a_structure)))->user_seconds = (a_value);}while(0)

#define pex_time_struct_get_user_microseconds(a_structure) (((struct pex_time*) (a_structure))->user_microseconds)

#define pex_time_struct_set_user_microseconds(a_structure,a_value) do {(((struct pex_time*)(a_structure)))->user_microseconds = (a_value);}while(0)

#define pex_time_struct_get_system_seconds(a_structure) (((struct pex_time*) (a_structure))->system_seconds)

#define pex_time_struct_set_system_seconds(a_structure,a_value) do {(((struct pex_time*)(a_structure)))->system_seconds = (a_value);}while(0)

#define pex_time_struct_get_system_microseconds(a_structure) (((struct pex_time*) (a_structure))->system_microseconds)

#define pex_time_struct_set_system_microseconds(a_structure,a_value) do {(((struct pex_time*)(a_structure)))->system_microseconds = (a_value);}while(0)

#define sizeof_pex_time (sizeof(struct pex_time))
#define sizeof_cl_option_handlers (sizeof(struct cl_option_handlers))
#define sizeof_cl_optimization (sizeof(struct cl_optimization))
#define sizeof_gcc_options (sizeof(struct gcc_options))
#define plugin_info_struct_get_version(a_structure) (((struct plugin_info*) (a_structure))->version)

#define plugin_info_struct_set_version(a_structure,a_value) do {(((struct plugin_info*)(a_structure)))->version = (a_value);}while(0)

#define plugin_info_struct_get_help(a_structure) (((struct plugin_info*) (a_structure))->help)

#define plugin_info_struct_set_help(a_structure,a_value) do {(((struct plugin_info*)(a_structure)))->help = (a_value);}while(0)

#define sizeof_plugin_info (sizeof(struct plugin_info))
#define sizeof_pex_obj (sizeof(struct pex_obj))
#define sizeof_context (sizeof(struct context))
#define set_htab_eq_pointer(a_value) htab_eq_pointer = (a_value);
#define set_htab_hash_pointer(a_value) htab_hash_pointer = (a_value);
#define set_libiberty_concat_ptr(a_value) libiberty_concat_ptr = (a_value);
#define set_plugin_event_name(a_value) plugin_event_name = (a_value);
#define set_plugin_is_GPL_compatible(a_value) plugin_is_GPL_compatible = (a_value);
