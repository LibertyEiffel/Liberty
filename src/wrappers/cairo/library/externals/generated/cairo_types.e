-- This file has been created by wrappers_generator.
-- Any change will be lost by the next execution of the tool.
deferred class CAIRO_TYPES


insert ANY undefine is_equal, copy end

	STANDARD_C_LIBRARY_TYPES

feature {ANY} -- C type definitions (typedefs)
       cairo_raster_source_finish_func_t: POINTER
               -- typedef cairo_raster_source_finish_func_t from /usr/include/cairo/cairo.h line 2673
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       cairo_raster_source_copy_func_t: POINTER
               -- typedef cairo_raster_source_copy_func_t from /usr/include/cairo/cairo.h line 2658
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       cairo_raster_source_snapshot_func_t: POINTER
               -- typedef cairo_raster_source_snapshot_func_t from /usr/include/cairo/cairo.h line 2640
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       cairo_raster_source_release_func_t: POINTER
               -- typedef cairo_raster_source_release_func_t from /usr/include/cairo/cairo.h line 2619
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       cairo_raster_source_acquire_func_t: POINTER
               -- typedef cairo_raster_source_acquire_func_t from /usr/include/cairo/cairo.h line 2600
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       cairo_surface_observer_callback_t: POINTER
               -- typedef cairo_surface_observer_callback_t from /usr/include/cairo/cairo.h line 2232
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       cairo_user_scaled_font_unicode_to_glyph_func_t: POINTER
               -- typedef cairo_user_scaled_font_unicode_to_glyph_func_t from /usr/include/cairo/cairo.h line 1867
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       cairo_user_scaled_font_text_to_glyphs_func_t: POINTER
               -- typedef cairo_user_scaled_font_text_to_glyphs_func_t from /usr/include/cairo/cairo.h line 1820
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       cairo_user_scaled_font_render_glyph_func_t: POINTER
               -- typedef cairo_user_scaled_font_render_glyph_func_t from /usr/include/cairo/cairo.h line 1750
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       cairo_user_scaled_font_init_func_t: POINTER
               -- typedef cairo_user_scaled_font_init_func_t from /usr/include/cairo/cairo.h line 1703
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       cairo_read_func_t: POINTER
               -- typedef cairo_read_func_t from /usr/include/cairo/cairo.h line 459
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       cairo_write_func_t: POINTER
               -- typedef cairo_write_func_t from /usr/include/cairo/cairo.h line 437
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       cairo_destroy_func_t: POINTER
               -- typedef cairo_destroy_func_t from /usr/include/cairo/cairo.h line 232
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

       cairo_bool_t: INTEGER
               -- typedef cairo_bool_t from /usr/include/cairo/cairo.h line 107
               -- Empty by design, used for anchored declarations.
       do
       ensure Result.is_default
       end

end
