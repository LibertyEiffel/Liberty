-- This file have been created by wrapper-generator.
-- Any change will be lost by the next execution of the tool.

deferred class LLVM_TYPES


insert ANY undefine is_equal, copy end

	STANDARD_C_LIBRARY_TYPES

feature {ANY} -- C type definitions (typedefs)
	llvmexecution_engine_ref: POINTER is
		-- typedef LLVMExecutionEngineRef
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	llvmvalue_ref: POINTER is
		-- typedef LLVMValueRef
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	llvmuse_ref: POINTER is
		-- typedef LLVMUseRef
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	llvmmemory_buffer_ref: POINTER is
		-- typedef LLVMMemoryBufferRef
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	llvmmodule_ref: POINTER is
		-- typedef LLVMModuleRef
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	llvmtarget_library_info_ref: POINTER is
		-- typedef LLVMTargetLibraryInfoRef
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	llvmgeneric_value_ref: POINTER is
		-- typedef LLVMGenericValueRef
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	llvmbool: INTEGER is
		-- typedef LLVMBool
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	llvmbasic_block_ref: POINTER is
		-- typedef LLVMBasicBlockRef
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	llvmtarget_data_ref: POINTER is
		-- typedef LLVMTargetDataRef
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	llvmpass_manager_ref: POINTER is
		-- typedef LLVMPassManagerRef
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	llvmmodule_provider_ref: POINTER is
		-- typedef LLVMModuleProviderRef
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	llvmstruct_layout_ref: POINTER is
		-- typedef LLVMStructLayoutRef
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	llvmtype_ref: POINTER is
		-- typedef LLVMTypeRef
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	llvmbuilder_ref: POINTER is
		-- typedef LLVMBuilderRef
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	llvmpass_registry_ref: POINTER is
		-- typedef LLVMPassRegistryRef
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	llvmcontext_ref: POINTER is
		-- typedef LLVMContextRef
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

end
