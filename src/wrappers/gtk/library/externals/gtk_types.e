-- This file have been created by wrapper-generator.
-- Any change will be lost by the next execution of the tool.

deferred class GTK_TYPES


inherit ANY undefine is_equal, copy end

feature -- C type definitions (typedefs)
	sig_t: POINTER is
		-- typedef sig_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gpoll_func: POINTER is
		-- typedef GPollFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_tree_iter_compare_func: POINTER is
		-- typedef GtkTreeIterCompareFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_entry_completion_match_func: POINTER is
		-- typedef GtkEntryCompletionMatchFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gclosure_marshal: POINTER is
		-- typedef GClosureMarshal
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gcopy_func: POINTER is
		-- typedef GCopyFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_clist_compare_func: POINTER is
		-- typedef GtkCListCompareFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_text_function: POINTER is
		-- typedef GtkTextFunction
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	cairo_read_func_t: POINTER is
		-- typedef cairo_read_func_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gint: INTEGER_32 is
		-- typedef gint
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_notebook_window_creation_func: POINTER is
		-- typedef GtkNotebookWindowCreationFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	timer_t: POINTER is
		-- typedef timer_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gnode_foreach_func: POINTER is
		-- typedef GNodeForeachFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_signal_func: POINTER is
		-- typedef GtkSignalFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_function: POINTER is
		-- typedef GtkFunction
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_clipboard_received_func: POINTER is
		-- typedef GtkClipboardReceivedFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsignal_emission_hook: POINTER is
		-- typedef GSignalEmissionHook
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	goption_parse_func: POINTER is
		-- typedef GOptionParseFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ptrdiff_t: INTEGER_32 is
		-- typedef ptrdiff_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gboolean: INTEGER_32 is
		-- typedef gboolean
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gclass_init_func: POINTER is
		-- typedef GClassInitFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ginstance_init_func: POINTER is
		-- typedef GInstanceInitFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_menu_position_func: POINTER is
		-- typedef GtkMenuPositionFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtype_plugin_use: POINTER is
		-- typedef GTypePluginUse
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_about_dialog_activate_link_func: POINTER is
		-- typedef GtkAboutDialogActivateLinkFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	cairo_user_scaled_font_init_func_t: POINTER is
		-- typedef cairo_user_scaled_font_init_func_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gcompare_func: POINTER is
		-- typedef GCompareFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gprint_func: POINTER is
		-- typedef GPrintFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gdk_native_window: NATURAL_32 is
		-- typedef GdkNativeWindow
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	pango_glyph: NATURAL_32 is
		-- typedef PangoGlyph
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtranslate_func: POINTER is
		-- typedef GTranslateFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_callback: POINTER is
		-- typedef GtkCallback
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gvoid_func: POINTER is
		-- typedef GVoidFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	grealloc_func: POINTER is
		-- typedef GReallocFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gfunc: POINTER is
		-- typedef GFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	glog_func: POINTER is
		-- typedef GLogFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gboxed_copy_func: POINTER is
		-- typedef GBoxedCopyFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gclosure_notify: POINTER is
		-- typedef GClosureNotify
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	goption_error_func: POINTER is
		-- typedef GOptionErrorFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_color_selection_change_palette_with_screen_func: POINTER is
		-- typedef GtkColorSelectionChangePaletteWithScreenFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_ctree_gnode_func: POINTER is
		-- typedef GtkCTreeGNodeFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_color_selection_change_palette_func: POINTER is
		-- typedef GtkColorSelectionChangePaletteFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	pthread_t: NATURAL_32 is
		-- typedef pthread_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	pango_cairo_shape_renderer_func: POINTER is
		-- typedef PangoCairoShapeRendererFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gcompletion_strncmp_func: POINTER is
		-- typedef GCompletionStrncmpFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gdestroy_notify: POINTER is
		-- typedef GDestroyNotify
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gasync_ready_callback: POINTER is
		-- typedef GAsyncReadyCallback
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_tree_view_mapping_func: POINTER is
		-- typedef GtkTreeViewMappingFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_type: NATURAL_32 is
		-- typedef GtkType
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_menu_callback: POINTER is
		-- typedef GtkMenuCallback
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gchild_watch_func: POINTER is
		-- typedef GChildWatchFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	cairo_user_scaled_font_unicode_to_glyph_func_t: POINTER is
		-- typedef cairo_user_scaled_font_unicode_to_glyph_func_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_destroy_notify: POINTER is
		-- typedef GtkDestroyNotify
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gfile_progress_callback: POINTER is
		-- typedef GFileProgressCallback
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	atk_key_snoop_func: POINTER is
		-- typedef AtkKeySnoopFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtraverse_func: POINTER is
		-- typedef GTraverseFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_window_keys_foreach_func: POINTER is
		-- typedef GtkWindowKeysForeachFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gint16: INTEGER_16 is
		-- typedef gint16
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	time_t: INTEGER_32 is
		-- typedef time_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gdk_atom: POINTER is
		-- typedef GdkAtom
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	clock_t: INTEGER_32 is
		-- typedef clock_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gint32: INTEGER_32 is
		-- typedef gint32
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_item_factory_callback1: POINTER is
		-- typedef GtkItemFactoryCallback1
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsize: NATURAL_32 is
		-- typedef gsize
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_object_init_func: POINTER is
		-- typedef GtkObjectInitFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gint64: INTEGER_64 is
		-- typedef gint64
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_module_init_func: POINTER is
		-- typedef GtkModuleInitFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_icon_view_foreach_func: POINTER is
		-- typedef GtkIconViewForeachFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gdk_pixbuf_destroy_notify: POINTER is
		-- typedef GdkPixbufDestroyNotify
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gshort: INTEGER_16 is
		-- typedef gshort
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gmodule_unload: POINTER is
		-- typedef GModuleUnload
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gioscheduler_job_func: POINTER is
		-- typedef GIOSchedulerJobFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gdata_foreach_func: POINTER is
		-- typedef GDataForeachFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_print_settings_func: POINTER is
		-- typedef GtkPrintSettingsFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ghook_compare_func: POINTER is
		-- typedef GHookCompareFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gfloat: REAL_32 is
		-- typedef gfloat
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gssize: INTEGER_32 is
		-- typedef gssize
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gregex_eval_callback: POINTER is
		-- typedef GRegexEvalCallback
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gintptr: INTEGER_32 is
		-- typedef gintptr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtype_class_cache_func: POINTER is
		-- typedef GTypeClassCacheFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_text_tag_table_foreach: POINTER is
		-- typedef GtkTextTagTableForeach
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	giconv: POINTER is
		-- typedef GIConv
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	cairo_user_scaled_font_render_glyph_func_t: POINTER is
		-- typedef cairo_user_scaled_font_render_glyph_func_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	pthread_spinlock_t: INTEGER_32 is
		-- typedef pthread_spinlock_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	pango_glyph_unit: INTEGER_32 is
		-- typedef PangoGlyphUnit
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	guintptr: NATURAL_32 is
		-- typedef guintptr
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_accel_map_foreach: POINTER is
		-- typedef GtkAccelMapForeach
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gdk_selection: POINTER is
		-- typedef GdkSelection
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	atk_event_listener_init: POINTER is
		-- typedef AtkEventListenerInit
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	clockid_t: INTEGER_32 is
		-- typedef clockid_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	atk_focus_handler: POINTER is
		-- typedef AtkFocusHandler
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gboxed_free_func: POINTER is
		-- typedef GBoxedFreeFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_page_setup_done_func: POINTER is
		-- typedef GtkPageSetupDoneFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsimple_async_thread_func: POINTER is
		-- typedef GSimpleAsyncThreadFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gdate_day: CHARACTER is
		-- typedef GDateDay
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_tree_model_filter_visible_func: POINTER is
		-- typedef GtkTreeModelFilterVisibleFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_tree_model_filter_modify_func: POINTER is
		-- typedef GtkTreeModelFilterModifyFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtoggle_notify: POINTER is
		-- typedef GToggleNotify
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_cell_layout_data_func: POINTER is
		-- typedef GtkCellLayoutDataFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ghook_check_func: POINTER is
		-- typedef GHookCheckFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ginterface_init_func: POINTER is
		-- typedef GInterfaceInitFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	goffset: INTEGER_64 is
		-- typedef goffset
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gdouble: REAL_64 is
		-- typedef gdouble
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_clipboard_clear_func: POINTER is
		-- typedef GtkClipboardClearFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gcompare_data_func: POINTER is
		-- typedef GCompareDataFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gchararray: POINTER is
		-- typedef gchararray
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_callback_marshal: POINTER is
		-- typedef GtkCallbackMarshal
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsource_func: POINTER is
		-- typedef GSourceFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gmodule_check_init: POINTER is
		-- typedef GModuleCheckInit
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gcallback: POINTER is
		-- typedef GCallback
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	glong: INTEGER_32 is
		-- typedef glong
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gulong: NATURAL_32 is
		-- typedef gulong
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_ctree_func: POINTER is
		-- typedef GtkCTreeFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gobject_finalize_func: POINTER is
		-- typedef GObjectFinalizeFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gdk_span_func: POINTER is
		-- typedef GdkSpanFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtype_plugin_complete_type_info: POINTER is
		-- typedef GTypePluginCompleteTypeInfo
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gpointer: POINTER is
		-- typedef gpointer
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ghrfunc: POINTER is
		-- typedef GHRFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gcompletion_func: POINTER is
		-- typedef GCompletionFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	pid_t: INTEGER_32 is
		-- typedef pid_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gfree_func: POINTER is
		-- typedef GFreeFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	-- GdkXEvent unwrappable: no wrapper type.
	gtk_tree_view_column_drop_func: POINTER is
		-- typedef GtkTreeViewColumnDropFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gcache_new_func: POINTER is
		-- typedef GCacheNewFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	guint32: NATURAL_32 is
		-- typedef guint32
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_text_buffer_serialize_func: POINTER is
		-- typedef GtkTextBufferSerializeFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	atk_state: NATURAL_64 is
		-- typedef AtkState
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	guint64: NATURAL_64 is
		-- typedef guint64
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gdk_wchar: NATURAL_32 is
		-- typedef GdkWChar
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gushort: NATURAL_16 is
		-- typedef gushort
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gdk_selection_type: POINTER is
		-- typedef GdkSelectionType
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_key_snoop_func: POINTER is
		-- typedef GtkKeySnoopFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_calendar_detail_func: POINTER is
		-- typedef GtkCalendarDetailFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	sig_atomic_t: INTEGER_32 is
		-- typedef sig_atomic_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gvalue_transform: POINTER is
		-- typedef GValueTransform
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_fundamental_type: NATURAL_32 is
		-- typedef GtkFundamentalType
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	cairo_destroy_func_t: POINTER is
		-- typedef cairo_destroy_func_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	pango_fontset_foreach_func: POINTER is
		-- typedef PangoFontsetForeachFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gdk_pixbuf_save_func: POINTER is
		-- typedef GdkPixbufSaveFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsocket_source_func: POINTER is
		-- typedef GSocketSourceFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ghook_finalize_func: POINTER is
		-- typedef GHookFinalizeFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtype_interface_check_func: POINTER is
		-- typedef GTypeInterfaceCheckFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	pango_attr_data_copy_func: POINTER is
		-- typedef PangoAttrDataCopyFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtype_plugin_unuse: POINTER is
		-- typedef GTypePluginUnuse
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gdk_destroy_notify: POINTER is
		-- typedef GdkDestroyNotify
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	locale_t: POINTER is
		-- typedef locale_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gcache_destroy_func: POINTER is
		-- typedef GCacheDestroyFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ghook_find_func: POINTER is
		-- typedef GHookFindFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	atk_function: POINTER is
		-- typedef AtkFunction
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_ctree_compare_drag_func: POINTER is
		-- typedef GtkCTreeCompareDragFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	size_t: NATURAL_32 is
		-- typedef size_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gbase_finalize_func: POINTER is
		-- typedef GBaseFinalizeFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_text_char_predicate: POINTER is
		-- typedef GtkTextCharPredicate
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gunichar: NATURAL_32 is
		-- typedef gunichar
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	atk_property_change_handler: POINTER is
		-- typedef AtkPropertyChangeHandler
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_tree_view_row_separator_func: POINTER is
		-- typedef GtkTreeViewRowSeparatorFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_clipboard_get_func: POINTER is
		-- typedef GtkClipboardGetFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	pango_attr_filter_func: POINTER is
		-- typedef PangoAttrFilterFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	guint: NATURAL_32 is
		-- typedef guint
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gconstpointer: POINTER is
		-- typedef gconstpointer
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsource_dummy_marshal: POINTER is
		-- typedef GSourceDummyMarshal
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	sighandler_t: POINTER is
		-- typedef sighandler_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_tree_model_foreach_func: POINTER is
		-- typedef GtkTreeModelForeachFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtype: NATURAL_32 is
		-- typedef GType
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gdk_input_function: POINTER is
		-- typedef GdkInputFunction
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_tree_destroy_count_func: POINTER is
		-- typedef GtkTreeDestroyCountFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	greg_t: INTEGER_32 is
		-- typedef greg_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	giofunc: POINTER is
		-- typedef GIOFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	guint8: CHARACTER is
		-- typedef guint8
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_tree_view_search_equal_func: POINTER is
		-- typedef GtkTreeViewSearchEqualFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_print_func: POINTER is
		-- typedef GtkPrintFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ghfunc: POINTER is
		-- typedef GHFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	cairo_write_func_t: POINTER is
		-- typedef cairo_write_func_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_tree_view_search_position_func: POINTER is
		-- typedef GtkTreeViewSearchPositionFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_clipboard_image_received_func: POINTER is
		-- typedef GtkClipboardImageReceivedFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gint8: CHARACTER is
		-- typedef gint8
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gunichar2: NATURAL_16 is
		-- typedef gunichar2
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gdate_year: NATURAL_16 is
		-- typedef GDateYear
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsignal_cmarshaller: POINTER is
		-- typedef GSignalCMarshaller
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_clipboard_urireceived_func: POINTER is
		-- typedef GtkClipboardURIReceivedFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_recent_filter_func: POINTER is
		-- typedef GtkRecentFilterFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_tree_selection_func: POINTER is
		-- typedef GtkTreeSelectionFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gdk_target: POINTER is
		-- typedef GdkTarget
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gbase_init_func: POINTER is
		-- typedef GBaseInitFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_translate_func: POINTER is
		-- typedef GtkTranslateFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_link_button_uri_func: POINTER is
		-- typedef GtkLinkButtonUriFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtest_log_fatal_func: POINTER is
		-- typedef GTestLogFatalFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_item_factory_callback: POINTER is
		-- typedef GtkItemFactoryCallback
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	guint16: NATURAL_16 is
		-- typedef guint16
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gpid: INTEGER_32 is
		-- typedef GPid
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_builder_connect_func: POINTER is
		-- typedef GtkBuilderConnectFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gquark: NATURAL_32 is
		-- typedef GQuark
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_file_filter_func: POINTER is
		-- typedef GtkFileFilterFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsequence_iter_compare_func: POINTER is
		-- typedef GSequenceIterCompareFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_assistant_page_func: POINTER is
		-- typedef GtkAssistantPageFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gchar: CHARACTER is
		-- typedef gchar
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	guchar: CHARACTER is
		-- typedef guchar
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gstrv: POINTER is
		-- typedef GStrv
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_tree_cell_data_func: POINTER is
		-- typedef GtkTreeCellDataFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_signal_marshaller: POINTER is
		-- typedef GtkSignalMarshaller
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	fpregset_t: POINTER is
		-- typedef fpregset_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gweak_notify: POINTER is
		-- typedef GWeakNotify
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gdk_filter_func: POINTER is
		-- typedef GdkFilterFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	uid_t: NATURAL_32 is
		-- typedef uid_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ghook_func: POINTER is
		-- typedef GHookFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	pthread_once_t: INTEGER_32 is
		-- typedef pthread_once_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	atk_event_listener: POINTER is
		-- typedef AtkEventListener
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_clipboard_targets_received_func: POINTER is
		-- typedef GtkClipboardTargetsReceivedFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gsignal_accumulator: POINTER is
		-- typedef GSignalAccumulator
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	va_list: POINTER is
		-- typedef va_list
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ghook_marshaller: POINTER is
		-- typedef GHookMarshaller
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gspawn_child_setup_func: POINTER is
		-- typedef GSpawnChildSetupFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gcache_dup_func: POINTER is
		-- typedef GCacheDupFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_module_display_init_func: POINTER is
		-- typedef GtkModuleDisplayInitFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_clipboard_rich_text_received_func: POINTER is
		-- typedef GtkClipboardRichTextReceivedFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_tree_selection_foreach_func: POINTER is
		-- typedef GtkTreeSelectionForeachFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gdk_event_func: POINTER is
		-- typedef GdkEventFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gclass_finalize_func: POINTER is
		-- typedef GClassFinalizeFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ghash_func: POINTER is
		-- typedef GHashFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gequal_func: POINTER is
		-- typedef GEqualFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gfile_read_more_callback: POINTER is
		-- typedef GFileReadMoreCallback
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_clipboard_text_received_func: POINTER is
		-- typedef GtkClipboardTextReceivedFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gobject_get_property_func: POINTER is
		-- typedef GObjectGetPropertyFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_item_factory_callback2: POINTER is
		-- typedef GtkItemFactoryCallback2
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	pthread_key_t: NATURAL_32 is
		-- typedef pthread_key_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_text_buffer_deserialize_func: POINTER is
		-- typedef GtkTextBufferDeserializeFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtime: INTEGER_32 is
		-- typedef GTime
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gscanner_msg_func: POINTER is
		-- typedef GScannerMsgFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_accel_group_activate: POINTER is
		-- typedef GtkAccelGroupActivate
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_class_init_func: POINTER is
		-- typedef GtkClassInitFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtype_plugin_complete_interface_info: POINTER is
		-- typedef GTypePluginCompleteInterfaceInfo
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_rc_property_parser: POINTER is
		-- typedef GtkRcPropertyParser
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	cairo_user_scaled_font_text_to_glyphs_func_t: POINTER is
		-- typedef cairo_user_scaled_font_text_to_glyphs_func_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	cairo_bool_t: INTEGER_32 is
		-- typedef cairo_bool_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_recent_sort_func: POINTER is
		-- typedef GtkRecentSortFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gthread_func: POINTER is
		-- typedef GThreadFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_accel_group_find_func: POINTER is
		-- typedef GtkAccelGroupFindFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ginterface_finalize_func: POINTER is
		-- typedef GInterfaceFinalizeFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ghook_check_marshaller: POINTER is
		-- typedef GHookCheckMarshaller
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gnode_traverse_func: POINTER is
		-- typedef GNodeTraverseFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gobject_set_property_func: POINTER is
		-- typedef GObjectSetPropertyFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	goption_arg_func: POINTER is
		-- typedef GOptionArgFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gtk_menu_detach_func: POINTER is
		-- typedef GtkMenuDetachFunc
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

end
