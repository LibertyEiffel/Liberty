/*
** This file have been created by wrapper-generator.
** Any change will be lost by the next execution of the tool.
*/
#define sizeof_obstack (sizeof( obstack))
#define timespec_struct_get_tv_sec(a_structure) ((( timespec*) (a_structure))->tv_sec)

#define timespec_struct_set_tv_sec(a_structure,a_value) do {((( timespec*)(a_structure)))->tv_sec = (a_value);}while(0)

#define timespec_struct_get_tv_nsec(a_structure) ((( timespec*) (a_structure))->tv_nsec)

#define timespec_struct_set_tv_nsec(a_structure,a_value) do {((( timespec*)(a_structure)))->tv_nsec = (a_value);}while(0)

#define sizeof_timespec (sizeof( timespec))
#define random_data_struct_get_fptr(a_structure) ((( random_data*) (a_structure))->fptr)

#define random_data_struct_set_fptr(a_structure,a_value) do {((( random_data*)(a_structure)))->fptr = (a_value);}while(0)

#define random_data_struct_get_rptr(a_structure) ((( random_data*) (a_structure))->rptr)

#define random_data_struct_set_rptr(a_structure,a_value) do {((( random_data*)(a_structure)))->rptr = (a_value);}while(0)

#define random_data_struct_get_state(a_structure) ((( random_data*) (a_structure))->state)

#define random_data_struct_set_state(a_structure,a_value) do {((( random_data*)(a_structure)))->state = (a_value);}while(0)

#define random_data_struct_get_rand_type(a_structure) ((( random_data*) (a_structure))->rand_type)

#define random_data_struct_set_rand_type(a_structure,a_value) do {((( random_data*)(a_structure)))->rand_type = (a_value);}while(0)

#define random_data_struct_get_rand_deg(a_structure) ((( random_data*) (a_structure))->rand_deg)

#define random_data_struct_set_rand_deg(a_structure,a_value) do {((( random_data*)(a_structure)))->rand_deg = (a_value);}while(0)

#define random_data_struct_get_rand_sep(a_structure) ((( random_data*) (a_structure))->rand_sep)

#define random_data_struct_set_rand_sep(a_structure,a_value) do {((( random_data*)(a_structure)))->rand_sep = (a_value);}while(0)

#define random_data_struct_get_end_ptr(a_structure) ((( random_data*) (a_structure))->end_ptr)

#define random_data_struct_set_end_ptr(a_structure,a_value) do {((( random_data*)(a_structure)))->end_ptr = (a_value);}while(0)

#define sizeof_random_data (sizeof( random_data))
#define sizeof_drand48_data (sizeof( drand48_data))
#define tm_struct_get_tm_sec(a_structure) ((( tm*) (a_structure))->tm_sec)

#define tm_struct_set_tm_sec(a_structure,a_value) do {((( tm*)(a_structure)))->tm_sec = (a_value);}while(0)

#define tm_struct_get_tm_min(a_structure) ((( tm*) (a_structure))->tm_min)

#define tm_struct_set_tm_min(a_structure,a_value) do {((( tm*)(a_structure)))->tm_min = (a_value);}while(0)

#define tm_struct_get_tm_hour(a_structure) ((( tm*) (a_structure))->tm_hour)

#define tm_struct_set_tm_hour(a_structure,a_value) do {((( tm*)(a_structure)))->tm_hour = (a_value);}while(0)

#define tm_struct_get_tm_mday(a_structure) ((( tm*) (a_structure))->tm_mday)

#define tm_struct_set_tm_mday(a_structure,a_value) do {((( tm*)(a_structure)))->tm_mday = (a_value);}while(0)

#define tm_struct_get_tm_mon(a_structure) ((( tm*) (a_structure))->tm_mon)

#define tm_struct_set_tm_mon(a_structure,a_value) do {((( tm*)(a_structure)))->tm_mon = (a_value);}while(0)

#define tm_struct_get_tm_year(a_structure) ((( tm*) (a_structure))->tm_year)

#define tm_struct_set_tm_year(a_structure,a_value) do {((( tm*)(a_structure)))->tm_year = (a_value);}while(0)

#define tm_struct_get_tm_wday(a_structure) ((( tm*) (a_structure))->tm_wday)

#define tm_struct_set_tm_wday(a_structure,a_value) do {((( tm*)(a_structure)))->tm_wday = (a_value);}while(0)

#define tm_struct_get_tm_yday(a_structure) ((( tm*) (a_structure))->tm_yday)

#define tm_struct_set_tm_yday(a_structure,a_value) do {((( tm*)(a_structure)))->tm_yday = (a_value);}while(0)

#define tm_struct_get_tm_isdst(a_structure) ((( tm*) (a_structure))->tm_isdst)

#define tm_struct_set_tm_isdst(a_structure,a_value) do {((( tm*)(a_structure)))->tm_isdst = (a_value);}while(0)

#define tm_struct_get_tm_gmtoff(a_structure) ((( tm*) (a_structure))->tm_gmtoff)

#define tm_struct_set_tm_gmtoff(a_structure,a_value) do {((( tm*)(a_structure)))->tm_gmtoff = (a_value);}while(0)

#define tm_struct_get_tm_zone(a_structure) ((( tm*) (a_structure))->tm_zone)

#define tm_struct_set_tm_zone(a_structure,a_value) do {((( tm*)(a_structure)))->tm_zone = (a_value);}while(0)

#define sizeof_tm (sizeof( tm))
#define sizeof_itimerspec (sizeof( itimerspec))
#define sizeof_sigevent (sizeof( sigevent))
#define set_program_invocation_name(a_value) program_invocation_name = (a_value);
#define set_program_invocation_short_name(a_value) program_invocation_short_name = (a_value);
#define set_signgam(a_value) signgam = (a_value);
#define set_stderr(a_value) stderr = (a_value);
#define set_stdin(a_value) stdin = (a_value);
#define set_stdout(a_value) stdout = (a_value);
#define set_daylight(a_value) daylight = (a_value);
#define set_getdate_err(a_value) getdate_err = (a_value);
#define set_timezone(a_value) timezone = (a_value);
#define set_tzname(a_value) tzname = (a_value);
