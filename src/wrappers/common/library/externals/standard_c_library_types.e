-- This file have been created by wrapper-generator.
-- Any change will be lost by the next execution of the tool.

deferred class STANDARD_C_LIBRARY_TYPES


inherit ANY undefine is_equal, copy end

feature -- C type definitions (typedefs)
	u_quad_t: NATURAL_64 is
		-- typedef u_quad_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	int8_t: CHARACTER is
		-- typedef int8_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	nlink_t: NATURAL_64 is
		-- typedef nlink_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	pthread_once_t: INTEGER_32 is
		-- typedef pthread_once_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	fd_mask: INTEGER_64 is
		-- typedef fd_mask
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ino_t: NATURAL_64 is
		-- typedef ino_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	comparison_fn_t: POINTER is
		-- typedef comparison_fn_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	off64_t: INTEGER_64 is
		-- typedef off64_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	dev_t: NATURAL_64 is
		-- typedef dev_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	fsfilcnt64_t: NATURAL_64 is
		-- typedef fsfilcnt64_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	pthread_t: NATURAL_64 is
		-- typedef pthread_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ushort: NATURAL_16 is
		-- typedef ushort
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	u_int: NATURAL_32 is
		-- typedef u_int
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	clockid_t: INTEGER_32 is
		-- typedef clockid_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	fsblkcnt64_t: NATURAL_64 is
		-- typedef fsblkcnt64_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	useconds_t: NATURAL_32 is
		-- typedef useconds_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	off_t: INTEGER_64 is
		-- typedef off_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	pthread_spinlock_t: INTEGER_32 is
		-- typedef pthread_spinlock_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	u_char: CHARACTER is
		-- typedef u_char
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	register_t: INTEGER_64 is
		-- typedef register_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	uid_t: NATURAL_32 is
		-- typedef uid_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	double_t: REAL_64 is
		-- typedef double_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	uint: NATURAL_32 is
		-- typedef uint
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	fsfilcnt_t: NATURAL_64 is
		-- typedef fsfilcnt_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	u_int8_t: CHARACTER is
		-- typedef u_int8_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	quad_t: INTEGER_64 is
		-- typedef quad_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	u_long: NATURAL_64 is
		-- typedef u_long
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	float_t: REAL_32 is
		-- typedef float_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	u_short: NATURAL_16 is
		-- typedef u_short
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	fsblkcnt_t: NATURAL_64 is
		-- typedef fsblkcnt_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	u_int32_t: NATURAL_32 is
		-- typedef u_int32_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ino64_t: NATURAL_64 is
		-- typedef ino64_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	blkcnt_t: INTEGER_64 is
		-- typedef blkcnt_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	u_int64_t: NATURAL_64 is
		-- typedef u_int64_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	gid_t: NATURAL_32 is
		-- typedef gid_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	int16_t: INTEGER_16 is
		-- typedef int16_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	key_t: INTEGER_32 is
		-- typedef key_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	caddr_t: POINTER is
		-- typedef caddr_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	time_t: INTEGER_64 is
		-- typedef time_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	timer_t: POINTER is
		-- typedef timer_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	locale_t: POINTER is
		-- typedef locale_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	suseconds_t: INTEGER_64 is
		-- typedef suseconds_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ssize_t: INTEGER_64 is
		-- typedef ssize_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	pid_t: INTEGER_32 is
		-- typedef pid_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	clock_t: INTEGER_64 is
		-- typedef clock_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	blksize_t: INTEGER_64 is
		-- typedef blksize_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	daddr_t: INTEGER_32 is
		-- typedef daddr_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	size_t: NATURAL_64 is
		-- typedef size_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	error_t: INTEGER_32 is
		-- typedef error_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	loff_t: INTEGER_64 is
		-- typedef loff_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	int64_t: INTEGER_64 is
		-- typedef int64_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	int32_t: INTEGER_32 is
		-- typedef int32_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	ulong: NATURAL_64 is
		-- typedef ulong
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	blkcnt64_t: INTEGER_64 is
		-- typedef blkcnt64_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	id_t: NATURAL_32 is
		-- typedef id_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	u_int16_t: NATURAL_16 is
		-- typedef u_int16_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	pthread_key_t: NATURAL_32 is
		-- typedef pthread_key_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

	mode_t: NATURAL_32 is
		-- typedef mode_t
		-- Empty by design, used for anchored declarations.
	do
	ensure Result.is_default
	end

end
