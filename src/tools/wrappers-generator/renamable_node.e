deferred class RENAMABLE_NODE
   -- A node of a GCC-XML file that may take a name different from those computed with the default rules.

inherit
   NAMED_NODE
   STORABLE_NODE

end -- class RENAMABLE_NODE
