system "leggow"
root LEGGOW: main
default -- section of the system
   assertion (require);
cluster -- section
   leggow: "./loadpath.se"
      default
         assertion (all)
      end
    
    lib: "../../lib/loadpath.se"
         default
             assertion (boost)
         end
    
    common_wrappers: "../../wrappers/common/loadpath.se"
    --     default
    --         assertion(boost)
    --     end
end
