deferred class LIBERTY_AST_REQUIRE_ELSE_VISITOR

inherit
	VISITOR

feature {LIBERTY_AST_REQUIRE_ELSE}
	visit_liberty_ast_require_else (v: LIBERTY_AST_REQUIRE_ELSE) is
		deferred
		end

end -- class LIBERTY_AST_REQUIRE_ELSE_VISITOR
