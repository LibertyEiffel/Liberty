deferred class LIBERTY_AST_ENSURE_THEN_VISITOR

inherit
	VISITOR

feature {LIBERTY_AST_ENSURE_THEN}
	visit_liberty_ast_ensure_then (v: LIBERTY_AST_ENSURE_THEN) is
		deferred
		end

end -- class LIBERTY_AST_ENSURE_THEN_VISITOR
