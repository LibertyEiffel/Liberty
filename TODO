 * Class renaming in ACE files (or the future Liberty equivalent:
   ASSET) => allows to simply inject factories while staying in a
   closed world assumption

 * Some pretty front-end to display messages, maybe with one or more
   implementations (SUI-like)

 * Plugins: some escape from the closed world

 * Keep precompiled types (using STORABLE)
